# User secrets - NEVER commit these
secrets.json
*.secrets.json

# Build outputs
bin/
obj/
publish/
dist/
node_modules/
dist-app
dist

# IDE
.vs/
.vscode/
.idea/
*.user
*.suo

# Logs
logs/
*.log

# OS files
.DS_Store
Thumbs.db

# Docker
docker-compose.override.yml

# Environment files
.env
.env.local
.env.*.local

# Generated on demand by `npm run gen:api` against a running backend.
# Not consumed by the app (see src/api/client.ts) — hand-written types in
# src/api/types.ts are authoritative. Regenerate to inspect the live contract.
src/api/schema.d.ts