# DeCloud Frontend Remake — Implementation Plan & Journal

**Companion to:** `FRONTEND_REMAKE_DESIGN.md` (the *what/why*). This document is the *how/when/status*, and doubles as the **running implementation journal** (§ Journal, bottom).
**Approach:** Option A — **split by URL**. Old vanilla app and new React app run side-by-side, separated by path at the server; pages are retired one at a time as they're re-imported into the new design.
**Version:** 1.1 — plan established and executed through Phase 3; §0, §8 and §9 are the living parts. (The original "1.0 — no code written yet" line survived far past its truth; corrected 2026-07-25.)

> **How to use this doc.** The plan (§1–§4) is stable scaffolding. The **Status dashboard** (§0) and the **Journal** (§8) are living — update them as work happens. Design rationale is *not* duplicated here; it lives in the design doc and is cited by section (e.g. "parity per DESIGN §7").

---

## 0. Status dashboard (living)

| Phase | Goal | Status | Owner | Started | Done |
|---|---|---|---|---|---|
| **Pre** | Backend serving split confirmed (§3) | ◑ Change 1 `Program.cs` delivered for sign-off; csproj target + new Vite project still needed (Phase 0) | Owner | 2026-07-21 | — |
| **0** | Scaffold new app beside old | ☑ Done — dev (`/app` via Vite) + Release/Production serving (`/app` from `dist-app`, `/` legacy) both proven | Owner | 2026-07-21 | 2026-07-21 |
| **0.5** | Visual direction → tokenize (Meridian) | ☑ Done — tokens (light+dark, AA-validated), fonts.css, reconciliation decided | — | 2026-07-21 | 2026-07-21 |
| **1** | Frozen core (session/identity + `api()`) | ☑ Done — full auth flow live (connect→SIWE→authenticated→shell), reload-persists; walletCrypto cross-compat verified; 54 tests | Owner | 2026-07-21 | 2026-07-22 |
| **2** | Shell + pipeline-proof page (SSH Keys) | ☑ Done — shell live; SSH Keys migrated + **legacy page retired in production** (first strangler retirement) | Owner | 2026-07-22 | 2026-07-22 |
| **3** | The spine (dashboard → deploy → VM detail) | ◐ In progress, spine functionally complete — VM **list** + **detail cockpit** live (status/services/access-info all live via SignalR, all four required a backend broadcast fix; metrics panel wired but gated on the node agent, which doesn't push yet). **Deploy live**: one-click + Customize (cpu/mem/disk, tier, bandwidth, GPU) with server-computed live price estimate. **Dashboard live**: balance/burn-rate/runway, live workload list, promoted Deploy. **Nav-of-record handoff EXECUTED** (signed-in `/` → `/app`) — ahead of Phase 5/6 on the plan, see §5. **Legacy dashboard + VM-list pages retired** (~490 lines) and **legacy create-VM modal retired** (~523 lines, including a third stale copy of the pricing tables). 110 tests. Remaining in-scope for calling Phase 3 done: template Variables in Deploy Customize (authoring-gated — the user-supplied-variable mechanism is proven and in use by the repo-deploy form; the generic per-variable form needs a template that declares *friendly* user variables, which authored templates bring — see 2026-07-26 journal); node-agent metrics push; ~~terminal/file-browser in-app routes~~ **DONE** — in-app **terminal** (xterm) + **file browser** (SFTP, full UI) shipped as full-page new-tab routes, and **wallet VM-password save/reveal** wired (§6.2). NOTE: SSH/SFTP works only on **composed-template** VMs; the **inline-template** VMs (web-proxy, stable-diffusion, postgres, code-server, …) ship no SSH provisioning, so terminal/file-browser can't reach them — full-catalogue access is blocked on the inline→composed **template migration** (deferred; see 2026-07-27 journal). **Direct Access (now Ports) + Custom Domains built as modal-routes (`/app/vms/:id/ports`, `/domains`) and verified live end-to-end — the §8 Phase-3 gap is closed (see 2026-07-25 journal)** | Owner | 2026-07-22 | — |
| **4** | Landing (SSG) built & proven | ☐ Not started | — | — | — |
| **5** | Supporting paths (marketplace, my-templates, nodes, wallet, profile, settings, admin) | ◐ Supporting paths complete (pending owner build-verify of the last two) — **Marketplace**, **template authoring + lifecycle + admin review/inspect**, **template earnings**, **Nodes** (my-nodes/search/detail + admin manager/inspect; legacy `marketplace.js` retired), **Wallet** (read-only page + native on-chain deposit/earnings-withdraw, Amoy-verified), **Profile**, and **Settings** (theme toggle live; language deferred to i18n) all shipped — see 2026-07-26 … 2026-08-07 journal. Only remaining supporting-path item is **i18n** (a separate R8 workstream, not a page). Also this stretch: a light-theme base-layer fix (native controls now follow `data-theme`, verified) and a node earnings/credentials cross-tenant leak closed server-side | Owner | 2026-07-26 | — |
| **6** | Cutover: `/`-flip, delete monolith + `window.*` bridge | ☐ Not started — **two pieces landed early**: (1) signed-in `/` already redirects to `/app` (a client-side JS behavior, not the server-side `/`-flip; see `BACKEND_SERVING_SPEC.md` §2.1); (2) **native on-chain deposit + earnings withdrawal now live in `/app`** (Wallet Slice 2, 2026-08-07), so the legacy app is no longer the *sole* place money moves — a key delete-monolith blocker is relaxed. **Residual legacy-only money-moves** (admin `withdrawPlatformFees`, frozen-contract migration, `withdrawBalance`) still gate full monolith deletion | — | — | — |

Legend: ☐ Not started · ◐ In progress · ☑ Done · ⚠ Blocked

**Test count history:** 34 (Phase 1 skeleton) → 54 (Phase 1 done) → 72 (VM list) → 80 (detail cockpit) → 90 (deploy sub-step 1) → 95 (deploy sub-step 2 Customize) → 103 (Customize tier/bandwidth/GPU + live pricing) → **110** (current — the `VmAction` numeric-ordinal fix added coverage). All green, `tsc --noEmit` clean, as of 2026-07-24.

---

## 1. The approach, and the constraint that forced it

**Option A — split by URL.** The old app keeps serving its paths; the new React app owns `/app/*`; ASP.NET routes between them.

**Why not "run both in one shell" (Option B islands).** Two routers can't co-own the URL. The old app isn't really routed (`showPage()` toggles `.active` on divs in one `index.html`), while React Router *does* own the URL — so co-mounting means they fight over paths and the back button. Worse, the old modules are entangled with the global document: `custom-domains.js` registers a document-level click listener at module load, inter-module calls go through `window.api`/`window.showPage`/`window.templateDetail`, and `index.html` has inline `onclick="showPage(...)"`. Making those safely mount/unmount as islands is fiddly adapter work that gets thrown away at cutover. **Option A sidesteps all of it**: the old app stays untouched, the new app is built cleanly beside it, and the migration end-state *is* the target-state (app under `/app/*`, landing at `/`) — not a temporary scaffold.

**Coexistence rule (who owns which path):**
- Old monolith: `/` and its existing assets — **primary entry point during migration**.
- New app: `/app/*` — grows page by page.
- Untouched standalone entries: `/sign.html`, `/report.html` (ported later, or left as-is until then).
- The landing (`/`) flip is **deliberately late** (§ Phase 4/6): moving the old app off `/` mid-migration is disruptive, so `/` stays the old app until the spine exists and it makes sense to send new users to a landing.

---

## 2. The retirement mechanism ("retire each page as we import it")

A page is migrated **atomically** — at any instant it's served by the old monolith *or* by React, never half-and-half, decided by URL. The steps per page:

1. **Build** the page as a React route under `/app/...`, talking to the real API through the new typed `api()`.
2. **Verify** against the design-doc parity inventory (DESIGN §7) for that page — the specific behaviors that must survive.
3. **Flip the entry point:** swing the link that used to open the old page so it now points at the new `/app/...` URL. (Early phases: the *old* sidebar link. After the dashboard migrates: the *new* shell's sidebar link — see the nav-of-record handoff, §4.)
4. **Delete** the old page's `<div id="page-...">` from `index.html`, its module (e.g. `ssh-keys` logic), its inline handlers, and any `window.*` exports only it used.

**Definition of "retired":** new page passes parity; old entry point points at the new URL; old div + module + inline handlers deleted in the *same* change (no dead code left beside the new file — DESIGN principle: tidy workspace).

**The `window.*` bridge to dismantle across phases** (each retired page removes the ones it owned; the rest go at cutover): `window.api`, `showPage`, `ethersSigner`, `templateDetail`, `handleDeployTosGate`, `showPasswordModal`, `copyToClipboard`, `_cdVerify`/`_cdRemove`, `marketplaceTemplates`, `sanitizeVmName`/`validateVmName`.

---

## 3. Precondition — the one non-frontend dependency (confirm before Phase 0)

Option A needs ASP.NET to serve **two front-end builds by path**. This is a small, real change to the serving layer (`Program.cs` / static-file + SPA-fallback rules), and it's the only piece that isn't pure frontend. Confirm with whoever owns the backend serving before Phase 0. Three distinct asks, needed at different times:

1. **Phase 0:** serve the new build under `/app/*` (with SPA fallback to the new `index.html` for client routing), while `/` and existing assets continue to serve the old app. Debug: Vite dev server for `/app`, .NET for API, as today.
2. **Phase 3:** teach the *old* monolith to **open to a given page on load** (read `?page=x` or `#x` → call `showPage(x)`). A few lines in the old app's init. Needed so the new shell can deep-link back into un-migrated pages after the nav-of-record handoff.
3. **Phase 4/6:** the `/`-flip — route `/` (+ locale prefixes) to the SSG landing, move/retire the old app's entry, keep `/app/*` as the SPA. (DESIGN §6.12 two-surface serving.)

---

## 4. Phases

Each phase lists **goal · tasks · exit criteria · what's retired · risk**. Tasks cite real files/modules so the work is concrete, not generic.

### Phase 0 — Scaffold beside the old app
- **Goal:** new React+Vite+TS build coexisting with the vanilla app; nothing user-facing changes.
- **Tasks:** new Vite+React+TS project emitting to a dist path the old build doesn't touch, served under `/app`; toolchain (tsconfig, ESLint/Prettier, Vitest, Playwright skeleton, CI perf-budget stub); design-token file skeleton (CSS custom properties — DESIGN §6.4) so components have tokens from day one; typed API client generated from `/swagger/v1/swagger.json` as a CI step (DESIGN §6.2); a hello-world `/app` page to prove the serving split (Pre-req #1).
- **Exit criteria:** `/app` renders a React page served by ASP.NET in Release and via Vite in Debug; old app at `/` unchanged; type-gen + tests run in CI.
- **Retires:** nothing.
- **Risk:** low. Main risk is the serving split (Pre-req) — that's why it's confirmed first.

### Phase 0.5 — Visual direction → tokens
- **Goal:** turn the chosen visual identity into the real token layer everything else consumes, before components get built against placeholder styles.
- **Status:** direction **chosen — "Meridian"** (cool architectural light; iris `#332ED6` accent; Space Grotesk / Inter / JetBrains Mono; centerless-mesh signature). Token seeds in DESIGN §6.4.
- **Tasks:** encode the Meridian seeds into `design-tokens.css` as role-based tokens (reconcile/replace the `design-tokens.css` that already exists in the frontend); **derive the dark mode** from the same roles (R7) and contrast-validate both; self-host the three OFL fonts (no CDN dependency); tighten the mockup into a small component reference (buttons, cards, rows, status dots, the mesh) that Phase 2's shell reuses.
- **Exit criteria:** a tokenized light+dark theme that passes contrast in both modes; the hero + dashboard mockup rebuilt from tokens (not hardcoded hex); fonts self-hosted.
- **Retires:** nothing (foundation).
- **Risk:** low. Gates token finalization so components aren't built twice.
- **Sequencing:** overlaps Phase 1 (the frozen core is logic, not looks) — can run in parallel. Must land before Phase 2's shell.

### Phase 1 — The frozen core
- **Goal:** the DESIGN §4 session/identity machine + `api()` proven correct as a *tested library*, before any page depends on it. This is the riskiest logic in the project (subtle, silently broken in rewrites), so it's built and frozen first.
- **Tasks:**
  - `api()`: bearer + `credentials:'include'` + on-401 refresh-retry-once; **single `ApiResponse<T>` unwrap** (boundary now uniform, DESIGN §12-Q2); typed.
  - Session/identity machine (DESIGN §4): derived `READY | NEEDS_CONNECT | NEEDS_AUTH | WRONG_NETWORK | ADDRESS_MISMATCH | UNCERTAIN` from (wallet, session, network); tri-state refresh (`true`/`false`/`null`); `signOutOnDisconnect`; account-switch A→B → NEEDS_AUTH (fail closed); wrong-network gates fund/escrow only.
  - SIWE/AppKit: port `siwe-config.js` hooks (nonce/verify/getSession/signOut) to the AppKit React adapter; `onAuthenticated` → session store.
  - `wallet-crypto.js`: port **verbatim** behind a hook (AES-GCM, wallet-derived key, session cache).
- **Exit criteria:** state machine + `api()` unit-tested green; can connect / sign-in (SIWE) / restore / refresh / sign-out / handle disconnect / account-switch / wrong-network — driven by a **test harness, no page built**. Assert: refresh token never read by JS; access token in memory + localStorage mirror; the three failure kinds (cancel/uncertain/definitive, DESIGN §5) behave.
- **Retires:** nothing (parallel to old app).
- **Risk:** medium — this is the crown-jewel logic. Mitigation: freeze + tests before anything builds on it.

### Phase 2 — Shell + pipeline-proof page (SSH Keys)
- **Goal:** prove the *entire* pipeline end-to-end on the lowest-stakes page before touching the spine.
- **Tasks:** `/app` shell (sidebar, header with balance placeholder, the derived-status gate → connect / re-auth / stale-banner; Radix + tokens; React Router v7 with the `/app` tree skeleton + admin-layout guard stub, DESIGN §3). **SSH Keys** page (table + add/delete via new `api()`, Radix modal for add-key) — chosen because it has no wallet-signing and no real-time, so it can't hurt anyone. Retire old SSH Keys by the §2 mechanism (swing the old sidebar link to `/app/settings/ssh-keys`; delete its div + `loadSSHKeys`/`openAddSSHKeyModal`).
- **Exit criteria:** SSH Keys fully works in React under `/app`, retired from the old app via link-swing; parity for that page passes; back button correct.
- **Retires:** **SSH Keys** (first cross-boundary retirement — validates the whole mechanism).
- **Risk:** low stakes by design; high learning value.

### Phase 3 — The spine (the product)
- **Goal:** build `connect → deploy → operate` — the reason the front-end exists (DESIGN §2). Retire old dashboard/deploy/VM pages as each lands.
- **Pre-task (grounding):** **close-read the live VM-status / metrics / lifecycle code** before building the operate cockpit, so it's drawn from the real state machine (flagged in DESIGN §2 / v0.7 note — the operate phase currently leans on the SignalR event names and modal surfaces, not a full read of the status paths).
- **Tasks:**
  - **Dashboard** = operate + fund home; **"Deploy" promoted** to a primary action (DESIGN §2). *Migrating the dashboard triggers the nav-of-record handoff — see below.*
  - **Deploy** at `/app/marketplace/:slug/deploy`: one-click "recommended settings" + opt-in Customize; preserve name validation (shared `VmNameService`), min-spec validation, platform-variable hiding, locked/editable/user constraints, ToS-gate retry, cost→runway, hard fund gate; land on the new VM detail page. Reuse the ONE deploy path logic from `deploy-submit.js`.
  - **VM detail** (`/app/vms/:id`) = operate cockpit: live status/metrics via **per-VM SignalR** (subscribe on mount / clean up on unmount, DESIGN §6.9); access panel (SSH/ports/domains/terminal as tabs/modal-routes); lifecycle stop/restart/destroy; runway indicator.
  - Implement Pre-req #2 (old app opens to a page via param).
- **Nav-of-record handoff (the fiddly moment):** while migrating, the old monolith is the entry at `/`. **The instant the dashboard is migrated**, the new shell becomes the natural home (it *is* the post-login landing), so: post-login now lands on `/app`; the new shell's sidebar links to *migrated* pages as client routes and to *un-migrated* pages by deep-linking into the old monolith (`/?page=x`, via Pre-req #2). Each subsequent migration swings one link from the old deep-link to the `/app/*` route.
- **Exit criteria:** a user can connect → (fund guard) → deploy with recommended settings in one click → land on the live VM detail and operate it, entirely in React; old dashboard/deploy/VM modules retired; spine parity (DESIGN §7) passes; the three money/lockout E2E flows green (DESIGN §8).
- **Retires:** dashboard, template deploy modal, VM list + VM modals (terminal/domains/access) as each lands.
- **Risk:** highest value + highest risk. Mitigations: core already frozen (Phase 1); operate close-read first; E2E gates on the money flows.

### Phase 4 — Landing (SSG) built & proven
- **Goal:** prove the **SSG pipeline** (distinct from the SPA pipeline proven in Phase 2) and have the landing ready.
- **Tasks:** build the SSG landing (build-time prerender, DESIGN §6.12) served at a **preview path** first (not `/` yet); tokened, light/dark, mobile-first, SEO/OG/`hreflang` scaffold, tight budget (must not pull the app bundle); one launch locale to start (i18n-ready arch, phased rollout — DESIGN §6.6); CTA → `/app`.
- **Exit criteria:** landing renders as static HTML at the preview path, passes its perf budget, excludes the app bundle; SSG build wired into CI.
- **Retires:** nothing yet (the `/`-flip is Phase 6).
- **Risk:** low. Decoupling "build landing" from "flip `/`" keeps it low-risk.

### Phase 5 — Supporting paths
- **Goal:** migrate the off-spine pages. **Parallelizable** — each is independent behind its own URL, so different people can take them concurrently once the spine is up.
- **Tasks (each via the §2 retirement mechanism):** Marketplace (`marketplace-templates.js` → URL-driven filters); My Templates (`my-templates.js`); Nodes (browse-only — **no detail page**, DESIGN §2); Settings (incl. SSH Keys already there from Phase 2); Admin (compliance/abuse/templates — `admin-*.js`, behind the layout role-guard); in-app `/app/report` reusing the report form; port `sign.html` if desired (or leave standalone).
- **Exit criteria:** every old page has a migrated `/app` equivalent; each old module + div + inline handlers deleted; parity per page passes.
- **Retires:** all remaining feature/admin modules.
- **Risk:** low-moderate, mechanical. The one care point: admin parity (CSAM NotScanned never shown as clean, etc. — DESIGN §7).

### Phase 6 — Cutover
- **Goal:** the old app is gone; the target topology is live.
- **Tasks:** the `/`-flip (Pre-req #3): `/` + locale prefixes → SSG landing; `/app/*` is the sole app. Delete the `index.html` page-div monolith, all remaining `window.*` bridge globals (§2), and inline handlers. Final full parity + E2E pass.
- **Exit criteria:** no vanilla monolith remains; DESIGN §8 success criteria met (feature-touches-one-place; critical-path speed; correctness-by-construction; zero known-behavior regressions).
- **Retires:** the old app, entirely.
- **Risk:** low if Phases 2–5 were disciplined (nothing left but deletion + the flip).

---

## 5. Sequencing notes & known wrinkles (honest)

- **Dual-shell transition period (Phases 2–3).** Before the dashboard migrates, clicking a migrated item from the *old* sidebar loads the new `/app` shell (its own sidebar) — so users briefly see the new shell for migrated pages and the old shell for the rest. This is the honest cost of incrementalism; it ends at the nav-of-record handoff (dashboard migration), after which the new shell is home and old pages are deep-linked.
- **Phases 4 and 5 are not strictly sequential.** The landing (4) and supporting paths (5) can proceed in parallel after the spine (3). Numbered for readability, not hard ordering.
- **`sign.html` / `report.html`** stay as standalone entries until explicitly migrated; they don't block anything and share only the wallet/token layers.
- **Locale launch set** is a product call (DESIGN §6.6, §14); the architecture carries all eight regardless of which launch first.

---

## 6. Working agreements & hard-won gotchas

Rules earned by debugging, not by theory. Each one cost real time; the examples are
named so they can be verified rather than taken on faith.

### 6.1 Ground before acting

Pull the real file before writing anything that depends on its shape. Endpoint paths,
DTO field names, enum members, constructor signatures, dependency import paths — all
have been guessed wrong at least once here, and grounding fixed each.

- `/api/ssh-keys` (guessed) 404s; `/api/user/me/ssh-keys` (grounded) is real.
- Deploy is **not** `POST /api/vms` — it is `POST /api/marketplace/templates/{id}/deploy`.
- There are **two** `VmsController`s (Orchestrator + NodeAgent). The frontend talks to
  the Orchestrator one only.
- `@noble` v2 import paths were guessed wrong three times; installing and probing settled it.

Corollary: on a module-resolution error, `npm ls <pkg>` is the *first* diagnostic, not the last.

**A search index is not the wire either (added 2026-07-25).** When these documents and the
repository are loaded into an assistant's project knowledge, that index can serve *stale copies
alongside current ones*. Observed directly: a single session returned two mutually exclusive
versions of `TemplateService.cs`/`ITemplateService.cs` — one declaring
`Task<List<VmTemplateSummary>> GetTemplatesAsync(...)`, one declaring `Task<List<VmTemplate>>`,
with matching pre-fix and post-fix bodies including the swallowed
`return new List<VmTemplate>()` that §6.10 records as fixed. Both cannot be on disk. Anyone
reading only the stale chunk would conclude a fixed bug is still live and "re-fix" it.
**Rule: search the index to find where something lives; read the checkout (or
`git show HEAD:<path>`) to know what it says.** This matters most for money-facing and
wire-facing code, which is exactly where the observed staleness landed.

**A negative grep proves nothing until the grep is proven.** When checking that something is
*absent* — a package missing from a bundle, a symbol with no callers — pair it with a positive
control you know should match. Used on 2026-07-25 to establish that axios and `ws` are absent
from `dist-app/assets/*.js`: the control was a SignalR string (`"Failed to start the
connection"`), which matched, showing the method could find something before its silence was
taken as evidence.

### 6.2 The dominant bug family: the mechanism exists, nothing calls it

By far the most common bug shape in this project — **seven** confirmed instances, one shape: capability present on both ends, connection missing between them.

| Symptom | Real cause | Fix |
|---|---|---|
| Cockpit never showed owner-initiated status changes | `TransitionAsync` changed status but only the node's `ReportVmStatus` broadcast | `IVmNotificationService.BroadcastStatusAsync`, called from `TransitionAsync` |
| Intermediate `Stopping`/`Provisioning` still missing after that fix | `VmService.PerformVmActionAsync` writes the optimistic status **directly**, bypassing `TransitionAsync` | Same seam, called from `PerformVmActionAsync` too |
| Services stuck at `Pending` forever | Heartbeat updated `vm.Services` and notified nobody | `BroadcastServicesAsync`, gated on the heartbeat handler's existing (previously-unused) `changed` return value |
| Cockpit's SSH/access panel never updated after the VM got its IP | Heartbeat populated `vm.AccessInfo` and notified nobody; the ONE method that does broadcast on access-info (`OrchestratorHub.ReportVmAccessInfo`) is only ever invoked by a node, which never calls it | New `ApplyReportedAccess` helper (extracted because the same block existed **verbatim** at two heartbeat call sites) returns whether anything changed; `BroadcastAccessInfoAsync` fires only then |
| Dashboard's live workload list never updated | `SubscribeToUser(userId)` has existed on the hub since it was written; nothing anywhere ever published to a `user:{id}` group — a subscription with no publisher | The status broadcast above now sends to **both** `vm:{id}` and `user:{ownerId}` in one call |
| One-click deploy failed: "missing BaseImageUrl" | The OS default lived only inside the legacy form; every other client sent an empty `ImageId` | Platform default applied at the one creation funnel (`VmService.CreateVmAsync`), tenant-scoped |
| A VM stayed billed-but-not-billing (`IsPaused=true`) forever after its owner topped up | The pause-clearing path only fired on an explicit `BalanceAdded` event, which apparently doesn't reliably arrive; the `VmStop` fall-through bills but never clears the flag | Self-heal: probe the actual balance on every billing cycle when the pause reason is specifically insufficient-balance, rather than wait for an event |

**Diagnostic:** when a feature works in the legacy UI but not the new one, the capability
exists — find the path the working client walks that the new one doesn't. Do not reimplement.
When nothing walks it either (the access-info and user-group cases), grep for the event
name across the whole backend before assuming a broadcast fires — `grep -rn "EventName"` is
faster than reasoning about whether it *should*.

**Fix shape:** when two or more code paths must both notify, extract *one* seam both call
(`IVmNotificationService`, one method per event type) rather than duplicating the emit. The
next path that changes that piece of state then gets live updates for free. Where the same
block of logic (not just the broadcast) is duplicated across call sites — the access-info
fields were set identically in two places in `NodeService` — extract that too
(`ApplyReportedAccess`), returning a change flag both the save-guard and the broadcast-guard
can share, rather than duplicating the diff logic as well as the emit.

**A fix to this family creates a new failure mode, and it bit us: change-gated broadcasts
lose events across a reconnect.** Every broadcast above is gated on "did the value actually
change" — necessary, or heartbeats would flood every client every few seconds. But
`withAutomaticReconnect()` doesn't replay missed events, and from the server's perspective
there's nothing to resend after a reconnect — nothing changed *since the last broadcast it
already sent*. A transition that happens entirely inside a disconnect window is gone, not
delayed. Fix: `HubProvider`'s `onreconnected` handler invalidates the relevant Query cache
keys, forcing a REST re-sync instead of trusting the socket caught everything. Verified live:
a reconnect was observed mid-VM-provisioning (a fresh WS `id=` appeared in the Network
panel — SignalR's own signature for "renegotiated"), and it happened to land the missed
events by luck of timing; the fix removes the luck requirement.

### 6.3 Trust the wire over the doc

An observed response beats a design doc, a memory, or a plausible inference.

- The design said a global `JsonStringEnumConverter` makes enums serialize as strings.
  `VmStatus` arrives as `3` — `VmStatus.cs` lacks the per-enum attribute `VmRole`/`VmCategory` carry.
  Two more enums turned out to have the same gap, found one at a time rather than all at once
  — `VmPowerState` (found while building the detail cockpit) and `VmAction` (found only when a
  **user manually curled a VM stop action** and got back a `400` — `"The JSON value could not
  be converted to VmActionRequest"` — because the cockpit's Stop/Start/Restart buttons had been
  posting `{"action":"Stop"}` the entire time the cockpit existed, and nothing had ever actually
  clicked one against production before that curl). **The lesson generalizes past this one API:**
  a "global converter is registered" fact does not mean every type it could apply to is
  actually annotated to use it, and the only way to know is to look at a real response, not
  reason from the registration.
- **A 404 and a 401 are different diagnoses.** Wrong path vs. wrong auth. The 401 on
  `/api/user/me/ssh-keys` *confirmed* the route existed.
- Normalize at the boundary, once. `normalizeStatus` was written for a list badge and
  later absorbed the SignalR path for free — a type error became a correctness fix. The same
  module (`src/features/vms/vmStatus.ts`) now owns all three ordinal-tolerant normalizers
  (`normalizeStatus`, `normalizePowerState`, `vmActionOrdinal`) — one file that knows the wire
  quirk exists, rather than three ad-hoc workarounds scattered near their call sites.
- **The single most expensive mis-trust of the wire this project produced was believing "the
  legacy UI's numbers are stale" without checking.** They weren't — the legacy tier-price
  table matched the backend's actual billing formula exactly; the real disagreement was
  between two places *inside* the backend (`HourlyRateCalculator` vs `SchedulingConfig`). An
  earlier, wrong claim to the contrary was corrected in the open (see the 2026-07-23 journal
  entry) rather than left standing. Checking both sides of a claimed disagreement, not just one,
  would have caught it faster.

### 6.4 Client pre-checks, server authority

The client avoids offering obviously-wrong options; the server decides. Held throughout
and never regretted: SSH-key validation, `allowedActions`, `specFloorErrors`, tier floors,
the fund gate.

Deliberately **not** done: mirroring the server's `ValidTransitions` graph; reimplementing
the pricing formula. The legacy UI does the latter client-side, which is exactly how a
client copy drifts from what billing actually charges.

**Ask what the foundation already provides before building.** Repeatedly the answer was
"it does": `POST /api/system/pricing/calculate` (same `HourlyRateCalculator` that bills),
`GET /api/system/images`, `GET /api/marketplace/platform-variables`, and an existing
`changed` flag in `UpdateServiceReadiness` carrying a subtle Ready-never-regresses guard.

### 6.5 Defaults are not declarations

`VmTemplate.MinimumSpec = new()` means a template that declares nothing still ships a full
spec of C# field defaults. On the wire, *"I require Standard"* and *"I said nothing"* are
byte-identical — so `?? no-constraint` fallbacks are unreachable.

Consequences seen and their resolutions:

- **Tier dropdown offered only two options** (Guaranteed/Standard) because the absent
  `MinimumSpec` fell back to `new VmSpec()`, whose `QualityTier` initialises to `Standard` — a
  field default acting as an unintended floor. **Resolved** by giving `platform-general` an
  explicit `MinimumSpec` (`QualityTier.Burstable` — the most permissive value; remember the
  enum is **inverted**, `Guaranteed=0` best … `Burstable=3` worst, so "most permissive" is the
  *highest* number) and `RecommendedSpec`, values chosen to match what the field defaults were
  already silently producing, so one-click deploy behavior didn't change.
- **Bandwidth dropdown collapsed to a single option** because the client (mirroring a bug the
  *legacy* modal has always had) read `template.defaultBandwidthTier` as if it were a floor —
  `?? 3` (Unmetered) meant a template that *defaults* to the best tier became impossible to
  *downgrade*. **Resolved** by separating the two properly: `allowedQualityTiers`/
  `allowedBandwidthTiers` (`src/features/deploy/useDeploy.ts`) now read the floor from
  `minimumSpec`, defaulting to *no constraint* (`0`/Burstable-equivalent) when absent, while
  `defaultBandwidthTier`/`RecommendedSpec` still seed which option starts selected. **A
  default seeds a selection. A minimum constrains choices. Never conflate them** — this is now
  a named, tested rule (`allowedBandwidthTiers` has a regression test asserting the old,
  wrong behavior no longer happens), not just a comment.
- **`ImageId` deliberately has no minimum, ever.** Only fields with a natural ordering belong
  in a minimum, and there is no "at least ubuntu-22.04." The correct fix for the platform's
  general-purpose template shipping VMs with no OS was a **platform default applied server-side
  at the one creation funnel** (`VmService.CreateVmAsync`, tenant-scoped so a system VM can
  never silently inherit the wrong base image) — not an entry in `MinimumSpec`, which would
  have quietly turned "the OS is a user choice" into "the OS is mandated."
- **Still open:** `MinimumSpec`/`RecommendedSpec` cannot express "unconstrained" at the type
  level (would need to be nullable `VmSpec?`). Every workaround above is a client-side or
  seeder-side compensation for that structural gap, not a fix to it. At least one existing
  template (a private-browser template, "Neko," found while auditing for this) has a
  `RecommendedSpec.QualityTier` that fails its own `MinimumSpec` floor check — a live
  contradiction the type system can't catch because "unset" and "set-to-the-wrong-thing" both
  type-check fine.

### 6.6 What each tool actually catches

| Tool | Catches | Misses |
|---|---|---|
| `vitest` | pure logic | anything rendered, anything over the network |
| `tsc --noEmit` | undefined symbols, wrong props, bad imports | hook ordering, runtime behaviour, wire-shape mismatches |
| `eslint-plugin-react-hooks` | conditional/misplaced hooks | — (**config written, scoped to hooks rules only — see below; not yet run against the codebase or wired into `npm run build`**) |
| Browser, **cold cache** | first-render crashes, wire-shape mismatches | — |
| Curling the real endpoint by hand | wire-shape mismatches no UI click ever exercised | anything the specific call doesn't cover |

Real escapes, in order of increasing subtlety: shipped components referencing CSS classes
that didn't exist (invisible Radix modal); `normalizeStatus` and `Row` used without import
(caught by `tsc`); **hooks placed after early returns** → React #310, which passed both
`vitest` and `tsc` and only crashed on a cold load; **every VM lifecycle button silently
broken from the moment the cockpit shipped** — `useVmAction` posted the action as a *string*
(`{"action":"Stop"}`) against an endpoint whose enum has no string converter (§6.3), and every
prior "confirmed live" status transition in this project had been triggered from the *legacy*
UI, not by clicking the new cockpit's own buttons — so nothing had ever actually exercised the
path. Found only because a user manually curled the endpoint for an unrelated reason and got a
`400` back.

**Rule 1:** hook placement is the whole question when adding a hook to a component with early
returns. Every hook goes above them; a `null` argument plus `enabled:` is how a query waits
for data. Adding it "next to the code that uses it" is wrong if that code is past a `return`.

**Rule 2, learned from the VmAction miss specifically: a green test suite and a working page
in the browser are not proof that a *write* action works.** Every read path in this app (list,
detail, live status) was exercised repeatedly by normal use. Write actions — a button that
POSTs — only get exercised by someone actually clicking that specific button, in that specific
state, against the real backend. **Before calling a mutation "done," click the actual button
once against the real API and read the Network tab response**, don't infer correctness from
"the surrounding page loads fine."

**`eslint-plugin-react-hooks` config, when installed, is deliberately narrow — hooks rules
only** (`rules-of-hooks: error`, `exhaustive-deps: warn`), not the full recommended set. A
mature codebase run through a fresh full lint config produces hundreds of findings —
including the deliberate `as any`/`as never` casts at the AppKit boundary — and a report
nobody reads is worse than no report. Broadening scope is a separate, later decision.

### 6.7 Delivery conventions

- **Replacement files: select-all → delete → paste.** Pasting at cursor has produced a
  doubled file and a cascade of redeclaration errors.
- **Ship a component's CSS with it.** New components use inline token styles rather than
  class names that may not exist yet.
- **Run `tsc --noEmit` on anything touching a component**, then load it cold in a browser.
- **Own mistakes in the commit message** when a later commit corrects an earlier claim.
- **A string-replace that matches must still be checked against *which* occurrence it hit —
  identical text can mean different things in different methods.** Two real incidents:
  replacing a `return new List<VmTemplate>()` guard hit the first textually-identical line in
  the file, which belonged to an unrelated method (`GetTemplatesByStatusAsync`) rather than the
  one being edited (`GetTemplatesAsync`) — caught by checking the surrounding method name after
  the fact, not by the replace itself. Separately, changing a controller's return type from
  `List<VmTemplate>` to `List<VmTemplateSummary>` via a blanket find-and-replace hit 13
  occurrences when only 2 were correct — the other 11 belonged to two *different* actions
  (`GetMyTemplates`, `GetPendingTemplates`) that call different service methods and correctly
  return full templates. **Rule: after any bulk replace, grep the result and read what method
  each hit landed in — don't trust that "the string matched" means "the edit was right."**
- **When cutting what looks like one contiguous block, verify nothing needed by a *survivor*
  lives inside it.** Retiring the legacy create-VM modal required deleting the range from
  `openCreateVMModal` to `createVM` — except `sanitizeVmName`/`validateVmName`/`previewVmName`
  sit physically *between* those two functions and are still consumed (via `window.*`) by two
  other legacy modules that were staying. The first attempt deleted them along with everything
  else; the fix was two separate cuts with the helpers preserved in between, guarded by an
  assertion that the cut range does *not* contain those three function definitions. The same
  root cause bit the dashboard/VM-list retirement (`loadNodes` sits inside what looked like one
  contiguous "dashboard stats" block, and is still used by the surviving Nodes page) and the
  bandwidth-tier constants cut. **The general check: before deleting a range, grep the range
  itself for anything with a caller outside it, not just for the thing you meant to delete.**
- **Two backend files in this codebase have mixed line endings** (`VmService.cs`,
  `NodeService.cs` — mostly LF with a handful of stray CRLF, or vice versa depending on which
  editor last touched which region). A patch built assuming one ending uniformly will fail to
  match with no useful error beyond "substring not found." Check both before assuming; a
  `.gitattributes` with `* text=auto eol=lf` would remove this class of friction entirely and
  is a cheap, not-yet-done fix.
- **A CSS media/network capture is worth more than a plausible narrative.** Every wrong
  hypothesis in the SIWE/reconnect/marketplace debugging below (§6.11) was corrected by an
  actual Network-tab or console capture, never by reasoning further about what the code
  *should* do. When a fix doesn't work, the next step is always "what does the wire actually
  show," not "what else could be wrong" reasoned from the armchair.

### 6.8 Config is a failure surface, and it fails silently

- The `--env-file` migration dropped `--ingress-domain`'s side effects
  (`INSTALL_CADDY`, `ENABLE_INGRESS`) → central ingress silently off, only a Debug-level log.
- Setting those internals *in* the env file instead produced an unstartable systemd unit
  (`Requires=caddy.service`).
- `appsettings.Production.json` overrides the base file — check both.
- A line-wrapped Mongo SRV URI crashed startup with a message naming nothing recognisable.
- `install.sh` reported success while the service was down.

**Rules:** env file holds *secrets only*; flags with installation side effects stay on the
command line. `install.sh` now rejects side-effect variables in an env file rather than
merely documenting the rule — see `install.env.example`.

### 6.9 The second client is a truth serum

Nearly every backend gap found during Phase 3 had existed for a long time. One client that
happened to walk the right paths was hiding all of them. Expect the migration to keep
surfacing latent bugs, and budget for fixing them — that is the migration doing useful work,
not scope creep. §6.10 and §6.11 below are two more, larger examples of the same thing found
later in Phase 3 — a marketplace endpoint timing out about 20% of the time, and a login modal
appearing on a valid session — both pre-existing, both invisible until the new app's traffic
pattern (or its author's habit of clicking around and reading the Network tab) exposed them.

### 6.10 A caught exception can make a real failure indistinguishable from empty data

`GET /api/marketplace/templates` intermittently returned `200 {"success":true,"data":[]}` or a
truncated JSON body. Root cause, once traced: the query returned **full domain objects**
(`VmTemplate`, including `CloudInitTemplate` — a multi-KB YAML body per template that no
listing renders), which pushed the response to ~1 MB; large enough that the Mongo driver's
socket read timed out mid-stream against the remote cluster, non-deterministically (a
consistent ~20s per attempt, so it failed roughly when the connection was slow that moment).

That alone would have been a clear 500. It presented as a **silent empty list** because **two
separate layers** — `DataStore.GetTemplatesAsync` and, one level up,
`TemplateService.GetTemplatesAsync` — each independently caught the exception, logged it, and
returned `new List<VmTemplate>()`. `MarketplaceController` already had correct behavior (throw
→ 500 `INTERNAL_ERROR`); it simply never got the chance, because the exception never reached
it. **Fixing only the lower layer would have produced a build error at the layer above and,
if "fixed" by matching return types without also removing the catch-and-swallow, no observable
change at all** — this is exactly what happened on the first pass; the second swallow was found
only because the compiler refused to build until it was addressed.

**The complete fix had three independent parts, and all three were needed:**
1. **Stop shipping data nothing renders.** A new `VmTemplateSummary` DTO (14 fields the
   marketplace grid actually reads, projected directly in the Mongo query so the excluded
   fields never leave the database) replaced `VmTemplate` on the two public listing endpoints
   only — mirroring the decision already made for VMs (`GET /api/vms` → `VmSummaryDto`, never
   the raw domain object). Payload dropped from 986,589 bytes to ~12,400 — a ~79× reduction —
   which alone made the timeout unreachable.
2. **Stop swallowing exceptions into a same-shaped success response**, at both layers. `throw;`
   instead of `return new List<...>()`, matching this codebase's own existing convention
   elsewhere in `DataStore.cs`.
3. **Do the filtering where the data lives.** `SearchTerm` and `Limit` were being applied
   *after* fetching every published template into memory — so `?limit=10` still transferred
   the whole collection before discarding the rest. Pushed into the Mongo query
   (`Regex.Escape`d search, server-side `.Limit()`), independent of the DTO change but found
   and fixed in the same pass.

**A fourth, unrelated bug was found as a side effect of grounding the fix.** The seeder
(`TenantVmTemplateSeeder`) checked whether a template slug already existed by scanning the
**public marketplace listing** — which is filtered to `Published`+`Public` — so a `Draft` or
`Private` template with the same slug was invisible to it, and re-running the seeder would have
attempted to create a duplicate. Fixed by looking up the slug directly
(`GetTemplateBySlugAsync`), which two other call sites in the same file already did — the
seeder's template-creation path was the outlier, not the pattern.

**Generalizable rule: `catch (Exception ex) { log; return <default>; }` is only safe when the
default is genuinely indistinguishable-in-consequence from "nothing to return" — for a public
listing endpoint, it almost never is, because the caller cannot tell "confirmed empty" from
"the query never ran."** Prefer `throw;` (or a typed result the caller must branch on) unless
there's a specific, stated reason the caller shouldn't see the failure.

### 6.11 Auth/network config is a build-time constant that fails silently and looks like the wrong bug

A user reported: navigating from `/app` to a legacy page and back always re-prompted for a
wallet signature, and dismissing with **X** left the session intact while **Cancel** signed the
user out. Three genuinely separate bugs were found in sequence, each ruled out only by an
actual capture, not by reasoning about the code:

1. **First hypothesis (wrong): refresh-token rotation between the two SIWE flows.** Disproven
   by capturing the `dc_rt` cookie value before and after visiting the legacy page — identical.
2. **Second hypothesis (wrong): a call-ordering race in the new session-restore code.**
   Disproven by adding `console.log` instrumentation to the actual `getSession` hook and
   observing it was never called with a bare, header-less request at all in the failing case —
   the earlier fix (see below) had already landed and taken effect.
3. **The real first bug, found by inspecting the actual outgoing request: `getSession` (the
   hook AppKit calls to decide whether to prompt for a signature) built its fetch through a
   local helper that set `Content-Type` and `credentials` but never an `Authorization` header** —
   against an endpoint that requires a bearer token. It had **always** 401'd, on every full page
   load, for the entire time this code existed; SPA navigation never reloads the bundle, so it
   was invisible until something forced a real page load (the legacy round-trip did, reliably).
   Fixed by having `getSession` answer from the app's own already-restored session state
   instead of querying the server independently — removing a second, racing source of truth
   rather than patching the first one to also succeed.
4. **The second real bug, found only after the first fix still didn't resolve the symptom, by
   logging the actual answer `getSession` was returning:** it was answering with the correct
   address but the **wrong chain ID** — `137` (Polygon mainnet) instead of `80002` (Amoy, what
   the platform actually runs on). `EXPECTED_CHAIN_ID` falls back to `137` when
   `VITE_EXPECTED_CHAIN_ID` is unset, and that variable lived **only** in a gitignored
   `.env.local` — so every server build had been silently mainnet-configured, and AppKit
   correctly treated a session on the "wrong" chain as inapplicable. This almost certainly also
   means the `WRONG_NETWORK` derived-status check had been comparing against the wrong chain in
   production since Phase 1, invisibly, because nothing surfaced it until AppKit's own
   session-applicability check used the same constant. Fixed with a committed `.env.production`.
5. **The third real bug, found by one more round of console instrumentation after the chain fix
   still didn't fully resolve it:** the session-answering callback read a `useRef` mirror of the
   session state, but `dispatch()` only *schedules* a re-render — it doesn't update the ref,
   which is written during render — so the ref was still `"anonymous"` at the exact moment the
   callback ran, even though the restore had genuinely already succeeded. Fixed by having the
   restore promise itself resolve **with** the recovered address, so the callback has a value to
   fall back to that doesn't depend on a render having happened yet.

**Every one of the three real bugs was invisible to `tsc` and to `vitest`** — they're about
runtime configuration and timing, not types or pure logic. Every one was found by adding a
`console.log` and reading the actual browser Network/Console output, not by re-reading the
source more carefully. **The generalizable habit: when a fix "should" work and doesn't,
instrument and capture before proposing a second fix** — three wrong-but-plausible hypotheses
were floated across this incident before the pattern of "capture first" was applied
consistently, and every capture disproved the previous guess outright rather than confirming it
with caveats.

**Structural lesson for anything build-time-configured going forward:** a client constant with
a *plausible-looking wrong default* (`|| 137`) is worse than one that fails loudly, because it
produces a working-looking app pointed at the wrong target rather than an error at build time.
The durable fix — not yet done — is fetching the chain ID from the backend (which already
knows it) rather than duplicating a deployment fact into a client build constant at all.

---

### 6.12 A check you have never seen fail is not known to work

Four safety nets were examined on 2026-07-25. Every one behaved differently from what
the team believed, and only one of the four beliefs was right:

- **ESLint** — documented as "written, not installed, not wired". Actually installed,
  wired into `npm run build`, and green. The docs were the only broken part.
- **`npm run typecheck`** — present in `package.json` since Phase 1, failing with `TS6310`
  before reading a single line of source. It had never once reported on this codebase.
- **`npm run gen:api`** — runs, succeeds, writes a file **nothing imports**. Output with
  no consumer looks identical to output that is being used.
- **`npm audit`** — the Phase 1 note recorded 4 findings with a guess ("build-time devDeps
  likely") that had carried weight unchecked ever since. The guess was roughly right; the
  count had grown to 11.

The shape is always the same: **a check that has only ever passed is indistinguishable
from a check that cannot fail.** Establishing which requires making it produce a *negative*
at least once, or proving it examined something:

- `eslint .` → `npx eslint . -f json`, count `filePath` entries (a zero-file lint prints the
  same clean output as a full one). 45 files.
- `tsc -b` → `--verbose` to see "Building" vs "up to date"; `--force` to defeat the cache.
- Incremental MSBuild targets → create the condition being detected (`touch package-lock.json`)
  rather than asserting the state you already have. See §8's `npm ci` item: the first test
  written for it returned "skip/skip" whether the target was wired correctly *or missing
  entirely*.
- Absence greps → pair with a positive control (§6.1).

**The corollary that cost the most this session: a commit message is a check too.** Four
messages in a row described work the commit did not contain — `c05fd1f` claimed a csproj
target and a `sourcemap` change that landed two commits later; `dd57868` claimed a
`.gitignore` rule that a missing trailing newline had silently glued to the previous line
and disabled; `8026008` described five files and touched two. Nothing was lost, but the
history now describes a project slightly different from the one in the tree. The ordering
that prevents it: `git add` → **`git --no-pager show --stat --cached`** → write the message
from what is actually staged. A message written from the plan is a prediction, not a record.

### 6.13 Patch delivery: base-drift on hot files, and two traps that produce a bad diff

Delivering changes as `git apply`-able patches (the delivery convention, §6.7) has three failure modes that recurred across the nodes/wallet/profile work and each cost a re-cut. All three are about the *base* the diff is taken against being wrong, not the change itself.

**(1) Base-drift on hot shared files.** A handful of files are edited nearly every session — `routes.tsx`, `AppShell.tsx`, the auth pair (`AuthProvider.tsx` / `walletState.ts`), `WalletPage.tsx`, `useTemplates.ts`, `MyTemplatesPage.tsx`, `DataStore.cs`. My shallow clone's `HEAD` has diverged from the owner's working tree on exactly these, so a patch cut against `HEAD` **rejects** — the surrounding context lines no longer match. The reliable fix is **the owner uploads the current file and I diff against those exact bytes**; self-contained *new* files never drift, so prefer adding a new module over editing a hot one when there's a choice. This bit on the auth files (Slice 2) and on `AppShell`/`routes` (sidebar/profile) and was fixed both times by re-cutting against the uploaded current version.

**(2) Reverse *this turn's* edits to reconstruct the base — not `git HEAD`.** When a patch is based on a file I already edited earlier in the same session, the base tree must be that file *with this turn's edits reversed*, not the pristine `HEAD` version. Cutting against `HEAD` re-bundles the earlier (already-applied) changes into the new patch, so applying it either conflicts or silently re-applies. Caught on the earnings backend (it re-bundled the Mongo index change) and on the modal-polish `WalletPage` (it re-bundled Slice 2).

**(3) On-chain / strict-TS code can't be eyeballed — it must be built.** ethers v6 types `Contract` methods as possibly-`undefined` (dynamic ABI), so under this project's strict TS every `escrow.frozen()` / `usdc.decimals()` is a compile error; the fix is explicit `UsdcContract` / `EscrowContract` interfaces + `as unknown as X`. I can't run `npm run build` here, so the first Slice-2 drop failed the owner's build on 14 such errors. **Money-moving and strict-typed code must be built and (for funds) testnet-verified by the owner before it's trusted** — "grounded + reviewed" is not "compiled."

**Mechanics that work:** reconstruct base/final trees in `/tmp`, `git diff --no-index base final | sed -E 's#(a|b)/(base|final)/#\1/#g'`, then `git apply --check` before delivery. **Shell gotcha:** a variable set inside a python heredoc is *not* visible to following bash lines — set `B=…` explicitly in bash.

### 6.14 Inline styles can't be responsive — the UI branches on `useMediaQuery`; the Meridian layer is the styling source

The app styles with inline React `style={}` objects, which **cannot carry `@media` rules**, so nothing is responsive by construction — a horizontal nav or fixed-width layout overflows on a phone and stays overflowed. Two ways out, both in use: (a) **fluid-by-default** layouts — `flex-wrap`, `gap`, and `grid-template-columns: repeat(auto-fit, minmax(…, 1fr))` reflow with no media query, and most page grids already use these; (b) where the structure must genuinely *change* (nav → drawer, multi-column → single), branch in JS with the **`useMediaQuery`** hook (`app/useMediaQuery.ts`, `MOBILE_QUERY = max-width 880px`) — the shell's hamburger drawer does this. **Rule for new UI:** prefer fluid primitives; reach for `useMediaQuery` only when the structure changes; never assume an inline-styled layout is responsive. And the **Meridian component layer** in `global.css` (ported from `meridian-reference.html`) is the styling source of truth — compose from `.card`/`.eyebrow`/`.status-dot`/`.badge`/`.mono`/`.track` + the display headings instead of hand-rolling inline card/label styles.

## 7. Definition of done (project-level)

From DESIGN §8, restated as the finish line: a feature or shared-component change touches **one** place; recommended-settings deploy is **one click** past the template; XSS is framework-owned (no hand-escaping in feature code); the §4 auth machine + `api()` are unit-tested and the three money/lockout flows are E2E-gated in CI; the §7 parity checklist passed at each retirement, so cutover carries **zero known-behavior regressions**.

---

## 8. Open items feeding the plan (from DESIGN §14 — kept in sync; DESIGN is authoritative on *why*, this list is what it means for sequencing)

**Resolved this session (2026-08-07), kept for history:**
- **Nodes — MIGRATED.** my-nodes + fleet search + owner-aware detail + admin manager/inspect + deploy-to-node shipped; legacy `marketplace.js` (all node code, despite the name) retired. See 2026-08-07 journal.
- **Template authoring + admin review — SHIPPED.** Authoring parity + lifecycle (publish / cancel-review / revise) + admin pending queue (approve/reject, reason required) + a read-only inspect page. Edit/inspect address by **`id`, never slug** (slug lookup is `Published`-only). See 2026-08-07 journal.
- **Template earnings — SHIPPED, from the settlement ledger** (`GET .../my/earnings`), not a stored counter. See 2026-08-07 journal.
- **Billing/payments writes — NO LONGER LEGACY-ONLY.** Native on-chain deposit + earnings withdrawal live in `/app` (`features/billing/paymentClient.ts`, ethers v6), **Amoy-verified**; `/app/wallet` + sidebar balance modal + `/app/profile` shipped. The DESIGN §7 "legacy app can't be deleted while it's the only place money moves" constraint is **relaxed** — residual money-moves listed under the new open items below.

**Resolved, kept for history:**
- **Surface undercount — RESOLVED (2026-07-21).** Six standalone entries found, not four. `terminal.html` + `file-browser.html` → in-app routes with pop-out preserved. `sign.html`/`report.html`/`tos.html` stay standalone. Folded into DESIGN v0.8.
- **Real-time — RESOLVED, live (2026-07-23/24).** All four scoped events (status/metrics/services/access-info) broadcast correctly; user-scoped dashboard list works; reconnect re-syncs. See IMPLEMENTATION §6.2 and DESIGN §6.9.
- **Tier pricing single-source — RESOLVED (2026-07-24).** `HourlyRateCalculator` reads `SchedulingConfig`; both deploy quote and actual bill share one formula. See DESIGN §12-Q4.
- **Marketplace listing payload/reliability — RESOLVED (2026-07-24).** See IMPLEMENTATION §6.10.
- **Auth/chain-ID modal bug — RESOLVED (2026-07-24).** See IMPLEMENTATION §6.11.

**New, surfaced building nodes / wallet / profile (2026-08-07) — tracked, none blocking:**
- **Atlas link latency — the highest-impact open item.** The Mongo crash-loop (2026-08-06 journal) was a `usageRecords` scan taking ~11.7 s to move 2,508 tiny records over a slow Atlas link, past the 10 s socket timeout. The crash is fixed (index `idx_settled_created` + socket timeout 10s→60s) but not the *cause* — ~1.1 MB in 11.7 s is **~95 KB/s effective**, dial-up-slow for Atlas, and it slows every ~1 MB query, not just that one. Most-likely culprits to measure before anything else: the Atlas **tier** (a shared M0/M2/M5 throttles throughput — would explain it), and the orchestrator↔cluster **region/path** (RTT vs throughput). Mitigations shipped, not cures: the `usageRecords` index; and (2026-08-07) projecting the ~48.9 kB inlined-artifact/cloud-init blobs off the My Templates list so it stops handing the slow link ~1 MB to render a name-and-status list. A `{NodeId:1,PeriodStart:1}` index on `usageRecords` would also speed the template-earnings scan.
- **✅ RESOLVED (2026-08-07) — node endpoints over-exposed secrets; fixed with a fail-closed `NodeView` DTO.** `GET /api/nodes/{id}` and the fleet `GET /api/nodes` serialized the internal `Node` model straight out, leaking earnings, `ApiKeyHash`/`CurrentJti`, **and** — found in production testing — system-VM auth tokens + ed25519/WireGuard **private keys**, the CGNAT relay token, and the relay private key to any authenticated caller. Took two passes: a field-scrub (fail-*open* — it missed the sub-object keys and was live-leaking them), then a **`NodeView` DTO** (fail-*closed* — a field ships only if projected; secrets in no tier, not even owner/admin). See 2026-08-07 journal. Both scrub patches superseded. Root lesson: don't return the internal persistence model from an API.
- **`HardwareInventory` shape unknown** (in `DeCloud.Shared`, not in this checkout) — node *detail* can't show CPU/GPU model, though search cards can (`NodeAdvertisement.capabilities`). Blocks closing the search-vs-detail gap.
- **Residual legacy-only money-moves (gate full monolith deletion):** admin `withdrawPlatformFees` (escrow `onlyOwner`); the frozen-contract migration path (native flow fails closed → points to the classic app); `withdrawBalance` (unused-deposit withdrawal — ABI present in `paymentClient`, UI unwired; distinct from earnings).
- **Repo/GitHub deploy doesn't consume `?node=`** — the deploy-to-node source chooser forwards the param but the repo-deploy page doesn't pin to it.
- **`templateForm` unit tests owed** — the authoring form's pure compose/validate helpers should get the test treatment the deploy slices got.
- **Per-page mobile responsiveness owed.** The *shell* is responsive (hamburger drawer, 2026-08-07), but the inline-style architecture means each page needs a pass for fixed widths, wide non-wrapping rows/tables (Wallet recent-usage / deposit-details, the template-inspect tables), long mono strings (escrow addresses), and multi-column form grids (Deploy, template form). Priority: Deploy + template form (densest), then Marketplace, Wallet, Nodes, VMs. Can't be render-verified in the sandbox — device/emulator testing required.
- **Landing-page responsiveness** — the landing is a separate SSG surface (Phase 4, not started), outside this app; tracked so "all UI mobile" isn't assumed done by the shell fix.

**Still open, blocking nothing further but tracked:**
- **Deploy Customize parity gaps** — template Variables (Marketplace-gated — see journal). (~~description cards~~, ~~OS image~~, ~~scheduling constraints~~, ~~replication factor~~ done 2026-07-25, see journal.) Needed to call Phase 3 fully done (§0).
- **Node-agent metrics push doesn't exist** — cockpit panel correctly hidden rather than shown empty; needs work on the NodeAgent side, a different codebase area than anything touched so far.
- **✅ RESOLVED (2026-07-25) — Direct Access (now "Ports") + Custom Domains built in the new app and verified live end-to-end.** Fixed exactly as this item called for: modal-routes off the VM cockpit — `/app/vms/:id/ports` (`DirectAccessModal`) and `/app/vms/:id/domains` (`DomainsModal`). The route is named **`/ports`, not `/access`**, honoring the naming caution at the end of this item. Live-testing quick-add then surfaced a *backend* port-allocation 401 that had been dark precisely because nothing exercised it (a §6.9 "second client is a truth serum"); root-caused across four layers and fixed — see the 2026-07-25 journal entry. *Original finding preserved for context:* **Direct Access and Custom Domains are unreachable to users in BOTH apps** (found 2026-07-25 by performing the "VM-modal cross-module audit" this item used to describe). The old wording — "orphaned code, not broken code; low risk; not yet confirmed safe to delete" — **had the direction wrong.** The audit was framed as *is this dead code we can delete?* and the answer is that it is not dead, it is **stranded**: since the create-VM-modal and VM-table retirements of 2026-07-24, nothing in either app can open these modals, while the backend endpoints (`/api/vms/{id}/direct-access`, `/api/central-ingress/vm/{id}/domains`) remain live. **Evidence.** `openDirectAccessModal` is defined at `direct-access.js:157` and called only at 403/440/466 — its own post-mutation refreshes — plus a `window.` assignment at 487; `openCustomDomainsModal` is defined at `custom-domains.js:43`, called only at 270, imported by `app.js:59` and assigned at `app.js:1369`. **Neither has a single external caller in any `.js` or `.html` file.** The `window.*` assignments existed for inline `onclick` handlers that were deleted along with the VM table. The modal markup still sits at `index.html:657` and `:700`, still wired to its close buttons — a modal nothing can open. **No replacement exists in the new app:** `wwwroot-next/src/features/vms/` contains only `VmsPage`/`VmDetailPage`/`useVms`/`vmStatus`, `VmDetailPage.tsx` contains no reference to either feature, and the sole match for `central-ingress|portMappings|custom.domain` anywhere under `wwwroot-next/src/` is the generated, unimported `schema.d.ts`. **Why this matters rather than being cosmetic — and the naming trap that hid it.** "Access" means two unrelated things in this codebase, and only one of them is broken. **`VmAccessInfo`** (in `VirtualMachine.cs` — `SshHost`/`SshPort`/`VncHost`/`VncPort`/`VncPassword`/`ConsoleWebSocketUrl`) is the VM's own SSH and VNC connection details; it is the "access" panel on `VmDetailPage`, it is live over SignalR, and **it works.** **`VmDirectAccess`** (a separate model in `DirectAccess.cs`, with its own `DirectAccessService` and its own endpoints) is Smart Port Allocation — a public subdomain plus port mappings from the 40000–65535 pool to VM-internal ports. Custom domains are a third thing again, under `/api/central-ingress`. **The two stranded features are the second and third; the first is fine, which is precisely why the gap is invisible:** the detail page looks complete, and what's missing never had a home on it. Concretely, a user today can see their VM is running, read its SSH host and port, and connect — but has no way anywhere to open port 25565 for a game server, expose 3306 for a database, or point their own hostname at the VM with automatic TLS. (`DirectAccessService.GenerateConnectionString` has explicit cases for `minecraft`, `mysql`, `postgresql`, `mongodb`, `redis`, `rdp` — that is the surface currently dark.) **This is a Phase 3 scope gap, not housekeeping** — it belongs with the Deploy Customize parity items above, and the fix is a route or panel in the new app, not a deletion. **Do not name it `/access`** — that collides with the working `VmAccessInfo` panel and would re-create the ambiguity that hid this; `/app/vms/:id/networking` or `/ports` keeps the two apart. Delete the legacy modules only once the replacement ships, at Phase 6 with the rest of the old app.
- **`window.copyToClipboard` is assigned twice, and which one wins is an accident of script order.** `app.js:1344` assigns an `async` implementation (defined at 1298); `direct-access.js:475` assigns a different, synchronous one; `index.html:923` loads `direct-access.js` as its own module script. Last write wins. Consumers include live inline `onclick` handlers in `ssh-wallet.js` (lines 194–333, the SSH key/cert/config copy buttons) and delegating wrappers in `vm-modals.js:15` and `custom-domains.js:15` that call through `window.copyToClipboard` when present. Nothing is visibly broken today, but the behaviour is unpinned. Found alongside the regression above; unrelated to it, and independent of whether the legacy modules are ever deleted.
- **Three enums serialize numeric despite the global string converter** (`VmStatus`/`VmPowerState`/`VmAction`) — client-tolerant now (`vmStatus.ts`); real fix has a blast radius into the legacy NodeAgent dashboard's integer-keyed lookup table, not yet scoped. **Blast radius re-measured 2026-07-25 and it is smaller than assumed, for two reasons.** (a) `VmNotificationService` hand-builds its SignalR payloads with `status.ToString()`, so the push path already sends names and is unaffected by adding the attribute — only the REST path changes. (b) A suspected *second* integer-keyed consumer, `wwwroot/src/status-helpers.js`, turned out to be dead code orphaned by the dashboard/VM-list retirements (see the 2026-07-25 journal entry); once deleted, the NodeAgent dashboard is the only consumer, as originally stated.
- **`MinimumSpec`/`RecommendedSpec` can't express "unconstrained"** (non-nullable `VmSpec`); at least one existing template ("Neko") recommends a tier below its own floor as a consequence.
- **Chain ID is a client build constant with a silently-wrong-looking-right fallback** — should be served by the backend instead. See IMPLEMENTATION §6.11.
- **Two independent SIWE/connect configs run simultaneously** (old app at `/`, new app at `/app`) until the `/`-flip. Not itself broken (all three real bugs found this way turned out to be self-contained), but worth checking first in any future auth-adjacent bug that reproduces after a legacy round-trip.
- **`eslint-plugin-react-hooks` — RESOLVED (2026-07-25), superseding the earlier "not yet run or wired" wording.** Installed, passing, and gating the Release build: `"build": "eslint . && tsc -b && vite build"`, and `BuildFrontendNext` runs `npm run build`, so a `rules-of-hooks` violation now fails `dotnet build -c Release` rather than reaching production. **The `exhaustive-deps` residual is closed too: a standalone `npm run lint` reports 0 errors and 0 warnings across 45 files.** The file count matters and was checked deliberately (`npx eslint . -f json`, count `filePath` entries): a lint that matches *zero* files prints exactly the same clean output as one that matches all of them, so "no output" is only evidence once you know something was actually examined. 45 is consistent with the app's `.ts`/`.tsx` count. Net: the effects were written carefully enough to have nothing to report, and the gate is armed for the next ones that aren't.
- **Error boundary shipped** (`RouteError.tsx` on the `/app` root route) — a `React Router` `errorElement`; catches render-time throws and unmatched routes with a styled page instead of the router's raw developer fallback. **Not yet wired to any error-reporting service** — deliberately, since none exists; a `console.error` is the current ceiling.
- **`install.sh --env-file` now rejects side-effect variables** rather than only documenting the rule (`install.env.example`) — closed as a mechanism, but worth re-reading if the installer script itself changes shape.
- **Mixed line endings** in `VmService.cs`/`NodeService.cs` — a `.gitattributes` normalizing to LF would remove a recurring patch-matching annoyance. Not started.
- **`npm ci` tracked by the lockfile — RESOLVED (2026-07-25), proven on the production server.** `RestoreFrontendNext` (and its legacy twin for `BuildFrontend`) run `npm ci` on `Inputs=package-lock.json` / `Outputs=node_modules/.package-lock.json`, replacing a condition gated on `node_modules` being *absent*. The old gate used "directory exists" as a proxy for "tree matches lockfile", so a stale-but-present `node_modules` silently skipped a newly added dependency and failed later with an unrelated-looking `TS2307`. **Measured on srv020184:** touching the lockfile makes the next build install (96s) and the one after skip (6s) — install-message count `1` then `0`. Both preconditions were verified before relying on them: npm writes `node_modules/.package-lock.json` on that box, with an mtime later than `package-lock.json`, for both the new app and the legacy one. The naive alternative (dropping the condition so `npm ci` always runs) was measured at **47.8s per build**, which is what made the incremental target worth having rather than just always installing. **The saving is real on every deploy**, not just manual builds: `install.sh` updates via `git pull` on a persistent checkout and `rm -rf`s `bin/`, `obj/`, `wwwroot/dist/` and `wwwroot/node_modules/.vite/` — but never `node_modules` itself. **Known unguarded edge, deliberately:** a *partially* deleted `node_modules` still has `.package-lock.json` and will be treated as current. The symptom is loud (`TS2307` at build) and the recovery is `rm -rf node_modules`; a mechanism to catch it would cost more than the failure does.
- **Source maps to production — RESOLVED (2026-07-25).** `vite.config.ts` now sets `sourcemap: false`; previously `/app/assets/*.map` was served, including a ~6.3 MB map for the main chunk, publishing full readable source and comments. Not a breach (client code is inspectable regardless) but a default rather than a decision. **No stale maps survived the change:** `emptyOutDir: true` is already set, so Vite wipes `dist-app` on each build even though `outDir` sits outside the project root — checked, and no `.map` files remain in either the build output or the published copy under `$INSTALL_DIR/publish`. `install.sh` does not clean `dist-app` and does not need to; adding a cleaner there would duplicate what the build already guarantees.
- **Main bundle is 1.71 MB (531 kB gzipped).** First-paint cost on `/app`, not a correctness problem. Route-level `React.lazy` is the obvious lever; natural to do alongside Phase 5 rather than on its own.
- **`npm audit`: 11 findings, 0 actionable as of 2026-07-25** — see the journal entry for the per-advisory reasoning and the two deliberate deferrals. **Standing rule: never `npm audit fix --force` here** — it downgrades `@reown/appkit-siwe` 1.8.23→1.6.5 and `react-router-dom` 7.18.1→7.11.0. **Expiry condition on the axios finding: if AppKit is upgraded, or `@base-org/account` stops being an optional dependency, rebuild and re-run the bundle grep** — the result is a property of the emitted bundle, so it must be re-measured whenever the dependency graph moves, not reasoned about.
- **Reown project-ID allowlist — RESOLVED (2026-07-25).** Dashboard checked: project identified and owned, usage/analytics consistent with our own traffic (no sign the open ID had been used by a third party), feature toggles reviewed, and the domain allowlist populated. `WALLETCONNECT_PROJECT_ID` verified correct in both `wwwroot/.env` and `wwwroot-next/.env.production`. **Carry-forward for Phase 6:** when the legacy app is deleted, retire or re-scope whichever project ID it was using.
- **OpenAPI-derived types — question settled 2026-07-25, adoption deliberately deferred to Phase 5.** The old wording ("regenerate + spot-check; the schema has drifted") assumed a link that does not exist: `npm run gen:api` writes `src/api/schema.d.ts` and **nothing imports it**. The only reference in the tree was a comment in `client.ts` promising types would come from it "once `gen:api` has run". So there is no drift to inspect — the hand-written interfaces are, and remain, the contract; `gen:api` is an on-demand tool for looking at the live backend, run against a *running* service on `:5050`. The file is now gitignored (it was untracked *and* unignored, which would have blocked a future `git pull` on the server the moment anyone committed it). **Adoption belongs to Phase 5**, when marketplace and pricing surfaces move and a silent contract change costs the most; generated swagger types are also awkward enough (everything nullable, `Record<string, never>` for empty bodies) that adopting is real work, not a switch. **The middle option — commit the file and diff it periodically — was considered and rejected: that is a ritual, and this project keeps replacing rituals with mechanisms.** **Accepted cost, stated rather than left looking solved: until Phase 5, backend contract changes surface as a runtime `undefined`, not a build error.** The three numeric enums above are an existing instance.
- **`npm run typecheck` was broken and had never run — RESOLVED 2026-07-25.** It was `tsc -b --noEmit`, which fails with `TS6310` ("referenced project may not disable emit") *before reading a line of source*, so the script every developer would reach for to answer "did that change break anything?" had never produced a verdict. Now `tsc -b`, matching what `build` already runs. **First real result: 0 errors on a forced full check** (`tsc -b --force` — the plain form was verified to be genuinely building, not skipping, via `--verbose`). Note it re-checks in full every run: `noEmit` means the expected outputs never appear, so build mode never considers the project up to date. Harmless in the safe direction — it can never falsely skip — but the "out of date because output file 'src/main.js' does not exist" line is permanent and is not a fault.
- Balance-change emit — deferred; balance/burn/runway all poll correctly (does not block anything).
- Landing content/copy — product task, needed for Phase 4 (not started).
- Launch locale set — product call, needed for Phase 4 (not started).
- App namespace hosting (`/app/*` same-origin vs future `app.` subdomain) — route tree unaffected; decide before Phase 6 if a subdomain is wanted.

---

## 9. Journal (living — newest first)

> Entry template:
> **`YYYY-MM-DD` · Phase N · <author>** — what changed / decided / learned; any status-dashboard update; any new blocker.

**`2026-08-07` · Design · Owner** — **Meridian design system ported into the app; shell reworked to a responsive top bar; nav IA regrouped.** The pages were functional but flat next to `meridian-reference.html` (default-font headings, plain cards, "● status" text) — because the reference's visual language lived only in that file, never in the app, so every page hand-rolled a flat version. **(1) Component layer.** Ported the Meridian primitives into `global.css`: `h1/h2/h3` now use the display face (Space Grotesk — the `@fontsource` fonts were already loaded, just never applied to headings), plus `.eyebrow`, `.card`/`.card-h`/`.card-row`, haloed `.status-dot`, tinted `.badge`, `.track`, `.stat`, `.mono` (tabular-nums), `.btn-ink`. Class vocabulary matches the pages (`status-dot`/`badge`/`mono` — reconciled after the owner hand-authored the first re-skin). **Re-skinned My Templates and Wallet** onto it, screenshot-verified (mono-tabular figures, real carded sections, the reference Balance card with a runway `.track` + mini-tiles). Marketplace cards also dropped three dead tokens (`--surface-0`/`--text`/`--radius-md`) by leaning on `.card` + the global input styling. **(2) Shell → top bar.** Replaced the sidebar with the reference's horizontal top bar (`AppShell`): wordmark + glowing dot + nav left; balance chip (mono + runway) + ProfileMenu + Deploy right; content centred. **(3) Nav IA regrouped** (owner spec): top-level nav is **Overview · Marketplace · Nodes · Virtual Machines · Admin ▾**. Relocations — **Wallet** left the nav (balance chip → `BalanceModal` → "View full wallet"); **SSH Keys + Settings** → ProfileMenu dropdown; **My Templates** → a **Marketplace tab** (Search | My Templates, `?tab=mine`, via an `embedded` prop on `MyTemplatesPage`; `/my-templates` list redirects to `/marketplace?tab=mine`); **Admin** pages → an Admin dropdown. **(4) Mobile.** The inline-style architecture can't carry `@media`, so responsiveness branches in JS: new `useMediaQuery` hook; under 880 px the top bar collapses to a hamburger + slide-in drawer (full nav / Admin / Deploy / balance). **This makes the SHELL responsive, not the whole app** — per-page responsiveness (fixed widths, wide rows/tables, long mono strings, form grids) is owed page by page, and the landing is a separate (SSG) surface. Files: `global.css` Meridian block; re-skinned `MyTemplatesPage`/`WalletPage`/`MarketplacePage`; reworked `AppShell` + new `useMediaQuery`; `ProfileMenu` (+SSH/Settings); routes redirect. Owner build-verified (an unused-import error caught + fixed); **device/click verification of the top bar + drawer still owed.** **Lesson: inline styles can't be responsive — branch on `useMediaQuery`, or move layout to CSS classes; and the Meridian layer in `global.css` is the styling source of truth, not per-component inline styles.**

**`2026-08-07` · Phase 5 · Owner** — **Template read paths: projected the heavy fields off the My Templates list; added an author view page; extracted shared inspect sections. VERIFIED.** Two threads off the Atlas-latency investigation. **(1) Payload.** `vmTemplates` averages ~48.9 kB/doc — seeded platform/system templates **inline** their artifact content (as `data:` URIs) and carry a large composed `CloudInitTemplate` (the whole base provisioning library is inlined into every composed doc) plus a `RoleCloudInit` layer. The My Templates list (`GET /api/marketplace/templates/my`) was returning **full** docs to render name/status/counts — ~1 MB over the ~95 KB/s link for the platform author. Fix: a **separate** `GetAuthorTemplateListAsync` that server-side exclude-projects `Artifacts`, `CloudInitTemplate`, `RoleCloudInit`, `LongDescription`, `AiAssessment`, wired only to the display endpoint. **Deliberately a new method, not a change to `GetTemplatesByAuthorAsync`** — that one also feeds *revise* (returns the fetched doc) and *archive* (re-saves it), so truncating it would wipe blobs; the caller check caught this. **Browse was already fine** (server-side `.Project` to `VmTemplateSummary`). Verified in production: list shows `cloudInitTemplate:""` / `artifacts:[]` / `roleCloudInit:""`, detail still full. **Drift lesson (again):** the first cut missed `RoleCloudInit` because that field was added to the model by the authored-template composition work that isn't in my checkout — I can't exclude a field I can't see; owner-uploaded current files are the only reliable base for these hot backend files. **(2) Author view.** New `/my-templates/:id` (`MyTemplateViewPage`) — read-only detail of the author's own template at **any** status (fetched by id, which is status-agnostic, so drafts open too), so you can inspect without having to Revise first; carries the full card action set (Edit / Submit / Cancel / Revise / Delete). The read-only body (specs / variables / artifacts+decode / ports / role-vs-composed cloud-init) was extracted from `AdminTemplateInspectPage` into a shared **`TemplateInspectSections`** (the `NodeSections` pattern) that both pages now render — admin inspect dropped ~250→~70 lines. `+ View` button on each My Templates card. Patches: `template-mytemplates-projection.patch` (+ the `RoleCloudInit` line), `mytemplates-view-button.patch`; new `TemplateInspectSections.tsx` / `MyTemplateViewPage.tsx`; `AdminTemplateInspectPage.tsx` refactor. Owed follow-up: a rejection-reason banner on the view page (pending confirming the field on the FE `VmTemplate` type). **This is a latency mitigation, not the cure** — the ~95 KB/s link is still the root (see §8).

**`2026-08-07` · Phase 5 · Owner** — **Node endpoints now return a fail-closed `NodeView` DTO — the earnings scrub was fail-open and had been leaking private keys.** Testing the earnings-scoping fix against production (curl as a non-owner) showed the scrub worked for the fields it named — but the *same* `GET /api/nodes/{id}` response still handed a non-owner the node's **system-VM auth tokens + ed25519/WireGuard private keys** (`SystemVmObligations`), the **CGNAT relay token + WireGuard config** (`CgnatInfo`), and the **relay private key** (`RelayInfo`). The scrub cleared six scalar fields and never touched the secret-bearing sub-objects — a live cross-tenant secret leak, and a direct demonstration of why field-scrubbing is the wrong shape: it protects only what you remember to list. **Root cause: the endpoint serialized the internal persistence model (`Node`) straight out — the leak was the model *shape*, not a field.** Fix: `NodeView` (+ `RelayView`/`DhtView`/`BlockStoreView`), a **fail-closed** projection where a field reaches a caller only if explicitly listed, so a new `Node` field can't leak by default. Secrets are in **no** tier — not owner, not admin (keys live on the node); `RelayView` drops `WireGuardPrivateKey`; `cgnatInfo`/`systemVmObligations` aren't projected at all (the client reads the `isBehindCgnat` bool, never those objects). Owner/admin still get the full operational tier; non-owners get the marketplace tier. Field membership was traced against exactly what the node pages render, same camelCase names, so the frontend parses unchanged. **Verified in production:** on a node the caller owns, `relayInfo` carries the public key but no private key and no obligations/cgnat; on a node they don't own, only the marketplace tier returns. `NodeView.cs` + `node-view-dto.patch` (removes the scrub method, net −40). **Supersedes both scrub patches** (`node-earnings-owner-scoping.patch`, `node-secret-subobjects-scrub.patch`). **Lesson: never return the internal model from an API — project to a DTO; fail-open scoping on a rich model is a leak waiting for the next field.**

**`2026-08-07` · Phase 5 · Owner** — **Light-theme regression fixed at the base layer: native form controls were following the OS, not the app. VERIFIED.** Switching the new theme toggle to light exposed form controls (checkboxes, number steppers, `<select>`s, and the repeatable list-row inputs) rendering dark on the light page. **Grounded — and the "hardcoded dark styles" framing was wrong:** the stylesheets carry no dark hex (all dark values are correctly scoped under `[data-theme="dark"]`), and components carry no inline dark hex either. The real cause was two base-layer gaps. (1) **`color-scheme` was never pinned** — nothing set it on `:root`/`body`, so the browser painted *native* controls (checkbox boxes, number steppers, select chrome, the default background of any un-styled input/select) using the **OS** appearance; OS-dark + app-light = dark controls. Fixed by pinning `color-scheme: light` on `:root`, `dark` under `[data-theme="dark"]` and in the `prefers-color-scheme` fallback. (2) **Only `.field input/textarea` was themed** — `<select>` was styled nowhere, and inputs built without `.field` (the Nodes filter bar; the repeatable rows for env vars / ports / user variables / artifacts) fell through to raw native rendering. Fixed by theming `select` / `textarea` / text-like `input`s with the same surface as `.field` inputs, plus `accent-color` on checkboxes/radios. Additive (+42/−0, role tokens only); inline styles still override. **The base layer is now the single source of form-control styling** — the prior state ("only `.field input` styled") was the incomplete version. **Verified live: light mode renders correctly across the screens that regressed** (Nodes filter/sort, Deploy selects, New-template repeatable rows). Patch: `theme-native-controls-fix.patch`. **Lesson: an un-styled native control follows the OS `color-scheme`, not your `data-theme` — pin it.**

**`2026-08-07` · Phase 5 · Owner** — **Settings shipped — the last un-migrated supporting page; language honestly deferred.** `/app/settings` (`SettingsPage` + `useTheme`) closes Phase-5 supporting paths. The one genuinely functional control is the **theme toggle** (Light / Dark / System): the token layer already ships `[data-theme]` and `index.html` has a pre-paint script reading `localStorage["dc-theme"]`, so `useTheme` just writes that exact contract (light/dark pin the key; **System** removes it and resolves via `prefers-color-scheme`, tracked live). The rest is navigation to surfaces that already exist (Profile, SSH Keys) + a ToS link. **Language is deliberately NOT a live selector** — the app has no i18n layer, so a picker would be UI for data that doesn't exist (same call as the deferred deploy "template Variables"); shown as "arrives with localization." Wiring swung the legacy `?page=settings` sidebar link to a client `NavLink` (the "swing the link as the page lands" pattern) and added the `settings` route. New files `features/settings/{SettingsPage.tsx,useTheme.ts}`; `settings-page-wiring.patch` cut against the owner's current `routes.tsx` + `AppShell.tsx` (both drifted from my clone — §6.13). Owner to `npm run build` + click-verify the toggle persists across reload. **Phase-5 supporting paths are now complete** save i18n (a separate R8 workstream, not a supporting-path gap).

**`2026-08-07` · Phase 5 · Owner** — **Node earnings + credentials scoped to owner/admin — a cross-tenant leak closed server-side.** `GET /api/nodes/{id}` **and** the fleet-wide `GET /api/nodes` (which my-nodes calls) returned the full `Node` to any authenticated caller, exposing every operator's earnings (`PendingPayout`, `TotalEarned`) *and* credential-adjacent fields (`ApiKeyHash`, `CurrentJti`, API-key timestamps) — wider than the docs had flagged (which named only the detail endpoint and only earnings). `Node.WithoutOwnerPrivateData()` returns a **shallow copy** with those fields cleared, never mutating the shared cached instance in `DataStore.ActiveNodes` (the hot path returns that reference); the controller scopes both endpoints — owner (wallet claim vs `Node.WalletAddress`, case-insensitive) or admin gets the untouched object, everyone else the scrubbed copy. **This is now the real boundary; the UI's owner-gating becomes defense-in-depth, not the control.** **Known limit, commented in code: fail-*open* on future fields** — a new owner-private field must be added to `WithoutOwnerPrivateData()`. A response DTO would fail closed but forces a coordinated frontend change; chose the non-breaking version and stated the tradeoff. Patch: `node-earnings-owner-scoping.patch` (+41/−2), applies to HEAD. Owner to `dotnet build` + verify a non-owner sees zeroed earnings. **[Superseded 2026-08-07 by the `NodeView` DTO — see the top entry. Production testing proved this scrub was fail-open: it missed the secret-bearing sub-objects and was leaking private keys. Kept here for the record.]**

**`2026-08-07` · Phase 6 · Owner** — **Wallet & shell-identity polish: one shared, legacy-quality deposit modal; sidebar balance card; header profile menu; `/profile` page.** After the owner's "all the legacy UI felt much better," the deposit and balance modals were rebuilt to the legacy structure (icon header → info card → prominent full-width action) and **consolidated**: a single shared `DepositModal` (styled info card — Network / Contract as an explorer-linked green mono pill / Min deposit; large amount field; success state with a "View transaction ↗" link) is now used by both the Wallet page and the new balance modal, so there's one deposit experience and no duplicated on-chain logic. New shell surfaces: a **sidebar balance card** pinned bottom (`SidebarBalance` → compact `BalanceModal` with available/confirmed/unpaid + pending earnings + Deposit/Withdraw + "View full wallet"), and a **header profile menu** (`ProfileMenu`) — a deterministic address-hashed **avatar** gradient (no dependency) opening Profile / Log out. **`/profile`** (`useProfile` → `GET /api/user/me`) shows identity + **quotas as Current-vs-Max bars** (VMs / vCPU / memory / storage); **roles come from the session** because `UserProfileResponse` omits them. All on existing tokens, no new deps, no backend changes. **Delivery note (recurred all session, now §6.13):** `AppShell.tsx` / `routes.tsx` / the auth files are hot and my clone's `HEAD` has diverged from the owner's tree on them — patches cut against `HEAD` reject; the reliable fix is the owner uploading the current file and diffing against exact bytes. Self-contained new files never drift.

**`2026-08-07` · Phase 6 · Owner** — **Wallet Slice 2: native on-chain deposit + earnings withdrawal, faithfully ported from `payment.js`. FUND-MOVING CODE.** `features/billing/paymentClient.ts` (ethers v6) ports the legacy flows with every guard intact: **deposit** = assert network → **`frozen()`/`replacementContract` guard** (never transact against a migrated contract) → min-deposit → USDC balance → allowance-then-`approve` → `escrow.deposit`, with the Polygon EIP-1559 gas floors (×1.2, 30-gwei min, 50/30 fallback) ported verbatim; **earnings withdrawal** = `escrow.nodeWithdraw(0)`; plus `readOnChain` (userBalances + nodePendingPayouts) for display. The one wallet-layer change: expose the ethers **signer** (`getSigner()`) — it already existed internally for SIWE — on the `WalletAdapter` + auth context. **Deliberate deviation from legacy:** wrong network **fails closed** (asks the user to switch) rather than auto-adding a chain with guessed RPC params. Addresses come from `GET /api/payment/deposit-info` — nothing hardcoded; the wallet is the final signing gate. **Verified live on Polygon Amoy: a deposit and a withdrawal round-tripped on-chain.** **Two gotchas worth carrying:** (1) **ethers v6 types `Contract` methods as possibly-`undefined`** (dynamic ABI) → under strict TS every `escrow.frozen()`/`usdc.decimals()` errors; fixed by defining explicit `UsdcContract`/`EscrowContract` interfaces and casting `new Contract(...) as unknown as X`. (2) The build gate is real — the first build failed on exactly those 14 TS errors; **I cannot run `npm run build` here, so on-chain money code must be `dotnet`/`npm`-built and testnet-verified by the owner.** Left unwired deliberately: `withdrawBalance` (unused-deposit withdrawal, distinct from earnings — ABI present); admin `withdrawPlatformFees`; the frozen-migration path (points to the classic app).

**`2026-08-07` · Phase 5 · Owner** — **Wallet Slice 1: read-only `/app/wallet`.** Purely additive, all from existing endpoints (`useBalance` + a new `useDepositInfo` → `GET /api/payment/deposit-info`): available-balance hero + runway/burn (warns under `LOW_RUNWAY_DAYS`), breakdown (confirmed / pending deposits / unpaid usage / total), pending-deposits list (tx → explorer, confirmation progress), recent usage, and a deposit-details card (escrow address, network, min, confirmations). Deposit/Withdraw initially bridged to the classic app; Slice 2 (above) replaced the bridge. **Precision fix folded in:** per-cycle usage costs render at 4 decimals so a ~0.0008-USDC cycle doesn't round to `0.00` (balances stay at 2 decimals — right precision for held funds).

**`2026-08-07` · Phase 5 · Owner** — **Template earnings, from the settlement ledger — not a counter.** New `GET /api/marketplace/templates/my/earnings` → `{templateId: {net, gross, deploys}}`, and My Templates shows "Earned N.NN USDC · K paid deploys" (net = author cut after platform fee). **Grounded decision:** `DeploymentCount × TemplatePrice` is wrong (self-deploys are skipped when `OwnerId==AuthorId`, failed settlements, price changes). The truthful figure is summed from `usageRecords`: template fees are written by `SettleTemplateFeeAsync` with a **zero-length period** (`PeriodStart==PeriodEnd`) crediting the author's revenue wallet, so the query finds those, attributes each `VmId → VirtualMachine.TemplateId` (VMs are soft-deleted/retained, so history resolves), and sums `NodeShare` (net) + `TotalCost` (gross). Reading the ledger rather than a second stored tally means it can't drift from on-chain reality. **Perf caveat:** `usageRecords` has no `NodeId` index, so this scans that collection filtered by payee+period then joins via the indexed `VmId` — fine now; a `{NodeId,PeriodStart}` index if it grows.

**`2026-08-07` · Phase 5 · Owner** — **Nodes migrated to `/app` (slices 1–3), legacy node code deleted, node detail made owner-aware, deploy-to-node added.** `/app/nodes` = my-nodes + fleet search tabs (`useNodes`); **my-nodes filters client-side** by wallet against `GET /api/nodes` because **no owner-scoped endpoint exists**. **Node detail** (`/app/nodes/:id`) is **owner-aware**: owner → full sections + earnings; non-owner → trimmed availability (free-vs-total) + uptime — because `GET /api/nodes/{id}` returns earnings to *any* caller, so the UI gates what the endpoint doesn't (flagged as backend over-exposure, §8). **Admin:** `/app/admin/nodes` (fleet + `DELETE /api/nodes/{id}`, the **only** admin node action — `deregister` is node-self with a `node_id` JWT claim, and there is **no suspend** endpoint) + a full node inspect page. **Deploy-to-node:** "Deploy here" → `/deploy?node=` source chooser, threaded through to `DeployTemplateRequest.NodeId`. **Legacy `marketplace.js` retired** (all node code despite the name) with its `index.html`/`app.js` touch-points — verified zero dangling refs. **Open:** `HardwareInventory` shape (in `DeCloud.Shared`, not in checkout) blocks CPU/GPU on the *detail* page (search cards have it via `NodeAdvertisement.capabilities`); repo-deploy doesn't yet consume `?node=`.

**`2026-08-07` · Phase 5 · Owner** — **Template authoring shipped: parity fields, full lifecycle, admin review + inspect.** My Templates gained the authoring **parity** fields and the **lifecycle** — `PATCH templates/{id}/publish` (Draft/Rejected → PendingReview if `IsCommunity` else Published), `cancel-review`, `revise` (community-Published → a new Draft revision with `ParentTemplateId`); `IsCommunity = !isAdmin`. **Admin review:** a pending queue (`GET /templates/pending`, `Authorize(Roles=Admin)`) with approve/reject (reason required), plus a dedicated **read-only inspect page** (`/app/admin/templates/:id`: tabbed role/composed cloud-init, variables table, artifacts with full SHA + base64 decode) — inspect is deliberately *not* the author edit form. **Grounded correctness fix:** Edit/Inspect must address templates by **`id`, never slug** — `GetTemplateBySlugAsync` filters `Slug==slug && Status==Published`, so drafts are unresolvable by slug and slugs collide across authors; the earlier `t.slug || t.id` links 404'd on drafts and were changed to `t.id`.

**`2026-08-06` · Ops · Owner** — **Mongo crash-loop resolved; the root *cause* of the slow link is the top open item.** The orchestrator was crash-looping on startup. Root cause: a `usageRecords` query (2,508 tiny records) took **11,753 ms** to transfer over a pathologically slow Atlas connection, past the 10 s socket timeout → unhandled → crash. Two fixes shipped: a compound index `idx_settled_created {SettledOnChain:1, CreatedAt:-1}` auto-created in `DataStore`, and the Mongo **socket timeout raised 10 s → 60 s**. **Unresolved and most operationally important:** *why* the Atlas link is that slow for so little data — the timeout bump is a guard, not a cure. A `{NodeId:1,PeriodStart:1}` index on `usageRecords` would also help the template-earnings scan (above).

**`2026-07-27` · Phase 3 · Owner** — **In-app file browser (SFTP) shipped — full-page new tab, rebuilt to match the legacy file-browser.html.** `FileBrowserPage` connects over the same proxy as the terminal (`/api/sftp-proxy/{vmId}?user=&password=&token=`, password path). JSON protocol: server sends `{type:"connected"}` → we drive `list`/`mkdir`/`delete`/`rename`; **download** is chunked (`download_start`→`download_chunk` base64→`download_complete`, reassembled to a Blob and saved); **upload** is the chunked handshake (`upload_start`→`upload_ready`→64 KB base64 `upload_chunk`→`upload_complete`, with `upload_progress`). UI matches the legacy: toolbar (Up/Refresh/Upload/New folder/Transfers), clickable breadcrumb, columned list (Name/Size/Modified/Perms) with single-click-select / double-click-open / right-click menu, a toggleable **transfer panel** with per-file progress bars (concurrent up+down keyed by `sessionId`), a context menu (Download/Rename/Delete), modal dialogs (no `window.prompt`), and drag-and-drop upload. Styled with Meridian tokens, not the legacy CSS. **Shipped in three passes** — MVP (list/download/manage) → upload → full-UI rebuild — the last two after owner feedback ("how to upload?" / "the old ui was better"). The one React trap handled: the once-bound `onmessage` reads a `pathRef`, not stale state, so re-lists after mkdir/delete hit the current directory. Verified live (browse/download/upload on a composed VM). **Same SSH/SFTP constraint as the terminal** — inline-template VMs can't be reached (see below). Retires legacy `file-browser.html` (deletion pending the legacy-app grep).

**`2026-07-27` · Phase 3 · Owner** — **VM root password is now wallet-securable and revealable — wiring the already-ported-but-unused `walletCrypto`.** The deploy password reveal gains **"Save encrypted (sign)"**: wallet-derive a key → AES-GCM encrypt → `POST /vms/{id}/secure-password`. The VM detail page gains **"Reveal password"**: `GET /vms/{id}/encrypted-password` → decrypt (sign) → show + copy. Exposed `signMessage` on the auth context (delegates to the wallet adapter). The envelope is byte-compatible with the legacy app, so passwords saved by either UI decrypt in both. **Fixes a real gap**: the new app's deploy reveal previously only *showed* the password and told you to copy it — it never saved it, so `WalletEncryptedPassword` stayed null and there was nothing to reveal later (this is why the terminal had no password to use). **Limitation**: a VM saved with a null encrypted password (shown-once, pre-this-change) can't be recovered — reveal says so; the fix is forward-looking (+ legacy-saved VMs work immediately).

**`2026-07-27` · Phase 3 · Owner** — **In-app terminal (xterm) shipped — full-page new tab — and the debugging surfaced the highest-impact open item. `TerminalPage`** connects over `/api/terminal-proxy/{vmId}?user=&password=&token=` (password path, matching the working legacy `terminal.html`; JWT in the `token` query param since a WebSocket can't send an auth header; raw frames for keystrokes, `{type:"resize",…}` JSON for resize). Added `@xterm/xterm` + `@xterm/addon-fit` (lockfile committed alongside for `npm ci`). Verified live: a real root shell on a composed-template VM. **Lesson — worth carrying (it recurred all session):** the terminal wouldn't connect with `"no suitable authentication method (publickey)"`; I diagnosed it as "the VM is publickey-only," switched the whole flow to an ephemeral-key path, then reverted it. **Both moves were wrong.** The password path was correct the entire time; the real cause was not the frontend at all. Two wrong turns from theorizing on one error message instead of testing against a VM known to work — the fix was a live test on a composed VM, which connected on the *original* code. **Trust the working client / the live system over a plausible model.** **Root cause (the deferred, high-impact item): templates split into two pipelines.** *Composed* templates (`platform-general`, `ai-chatbot`, `repo-deploy`) run `TemplateComposer.Compose("base-tenant.yaml", role)` and inherit SSH provisioning (root `chpasswd`, `PasswordAuthentication yes`, `ssh_pwauth`) + `qemu-guest-agent`. *Inline* templates (`stable-diffusion`, `flux`, `postgresql`, `code-server`, `private-browser`, `privacy-proxy`, `web-proxy`) set `CloudInitTemplate = @"#cloud-config …"` standalone → they inherit Ubuntu's cloud-image default (**publickey-only, no root password, no guest agent**) → **no SSH/SFTP path at all**, so terminal + file browser can't reach their VMs. Their `${DECLOUD_PASSWORD}` is the *app* password (e.g. nginx `htpasswd`), not SSH. **Deferred migration:** convert the inline templates to compose `base-tenant` (spans `TenantVmTemplateSeeder.cs` **and** the role cloud-init YAMLs in `DeCloud.Builds`, which isn't in this checkout) — the real unblocker for terminal/file-browser across the whole catalogue.

**`2026-07-26` · Phase 5 · Owner** — **Deploy is now a source chooser, "From GitHub" deploys for real, and grounding it corrected our model of template Variables.** The three Deploy buttons (shell + 2 dashboard) open `/app/deploy` — a chooser with three sources: **Empty workload** → `platform-general` deploy, **From marketplace** → browse, **From GitHub** → a new repo-deploy form. The **GitHub form** (`RepoDeployPage` + pure `repoDeploy.ts`) is a byte-exact React port of the legacy `repo-deploy.js`: friendly fields (URL / ref / port / database / env editor + `.env` paste / private-repo deploy key) composed into three base64 payloads — `DEPLOY_CONF_B64` (shell-quoted `KEY=value` block with a trailing empty line), `APP_ENV_B64` (env file, newline-collapsed values, trailing `\n`), `DEPLOY_KEY_B64` (optional, trailing `\n`) — and sent as `environmentVariables` on `platform-repo-deploy` via the existing `useDeploy`/`submitTemplateDeploy` path. The composition is pure and **unit-tested (8 byte-exact cases** — `shq` escaping, unicode-safe `b64` not bare `btoa`, the trailing-line details) because `provision.sh` dot-sources the payloads on the VM, so a byte off fails closed. Verified live: a real repo built and ran. **Correction to the replication entry's "zero user-facing variables" claim (it was too strong):** the `platform-repo-deploy` template *does* declare user-facing variables (`DEPLOY_CONF_B64` etc. — declared, resolver-less, filled from the user's `environmentVariables` → `UserSuppliedStatics` at render by `CloudInitRenderer`). So the **user-supplied-variable mechanism is real and in use** — the GitHub form is a consumer of it. The accurate statement is narrower: no *seeded* template declares **friendly, individually-editable** user variables (a lone `POSTGRES_PASSWORD`); repo-deploy's are opaque base64 blobs needing a bespoke composer, not the generic per-variable form. So **template Variables (Phase-3 gap #5) is authoring-gated, not "no user variables exist"** — same delivery channel (`environmentVariables`), different input UI; the generic form and the GitHub form are siblings.

**`2026-07-26` · Phase 5 · Owner** — **Marketplace shipped: browse + detail, retiring the last stale pricing tables and unhardcoding the shell.** **Browse** (`/app/marketplace`, `MarketplacePage` + `useMarketplace`): grid from `GET /templates` with URL-driven filters (search / category / gpu / sort via `useSearchParams` — first use in the app), a featured strip, and server-priced cards (`EstimatedCostPerHour` off the summary — never client-computed). **Detail** (`/app/marketplace/:slug`): full template, server price, reviews (`GET /reviews/template/{id}`), Deploy button; browse cards route here. Retires `marketplace-templates.js` and `template-detail.js` — the last live copy of the stale client-side pricing tables (detail prices from the server). **Descriptions render Markdown** (`react-markdown` + `remark-gfm`, raw HTML **disabled** → XSS-safe for untrusted third-party content by construction — the safe default *is* the fix, no `dangerouslySetInnerHTML`, no separate sanitizer) styled by a scoped `.md` class; **two deps added, so `package.json` + `package-lock.json` must be committed together** — the csproj `RestoreFrontendNext` target runs `npm ci`, which is lockfile-strict and fails on a manifest/lock mismatch. **Unhardcode**: the three `platform-general/deploy` Deploy links → the chooser, and the duplicate legacy Marketplace nav `<a>` retired. The owner's build caught two of my slips (a new test missing `import {…} from "vitest"`; `React.CSSProperties` unusable under `jsx:react-jsx` → `import {type CSSProperties} from "react"`) — both folded in. Verified live: a rich catalogue renders (Stable Diffusion, FLUX, PostgreSQL, vLLM, Minecraft, Coolify, + system templates), with filters, featured, detail, and reviews all working. **Legacy-file retirement** (`marketplace-templates.js` / `template-detail.js` / `repo-deploy.js`) still pending — they live in the legacy app (`wwwroot/src`), so deletion needs a grep for stray callers first; deferred as a careful legacy-app change, not rushed.

**`2026-07-25` · Phase 3 · Owner** — **Deploy Customize replication factor shipped (4 of 5 parity gaps) — and grounding the 5th (template Variables) found it's Marketplace-gated, closing Phase-3 Customize as far as it can go pre-Marketplace.** Replication: a "Durability" select over exactly the server-accepted set `{0,1,3,5}` (grounded against `VmService`'s clamp — anything else → 3), labelled Ephemeral / 1 / 3 / 5 copies so the data-loss tradeoff shows in the option text; chosen over a toggle+count so only valid values are offered. Seeds from the template's recommendation, else ephemeral, and — the same silent-downgrade fix as image/bandwidth/GPU — now **forwards `replicationFactor` in `customSpec`** (customising a durable template would otherwise drop it to ephemeral). Side effect: the always-dark "Replication" price row now appears once factor > 0, since the priced spec finally carries it. A test guards the client set == the server's `{0,1,3,5}`. 121 → 122. **Verified live, and deeper than the UI:** deployed at factor 1 and confirmed in Mongo — `Spec.ReplicationFactor: 1` (not the `0` default, not clamped), and the block-store manifest shows `LazysyncStatus` confirmed, `ConfirmedRootCid`/`ConfirmedVersion` matching, 1713 chunks seeded, manifest `ReplicationFactor: 1` — i.e. the disk is actually replicated, the machinery `factor==0` skips. The same VM carried `ImageId: debian-12` and `Constraints: [node.gpu.present eq true]`, so four slices are visible working together in one spec. **Template Variables — grounded, deliberately NOT built:** the discriminator is NOT `TemplateVariable.kind` (that's `VariableKind {Static,Dynamic}` — resolution timing; the frontend comment said otherwise and was corrected). Platform-vs-user is **resolver-key membership**: a declared variable is platform-managed if its name is a key from `GET /api/marketplace/platform-variables`, user-facing only if it has no resolver. But **every declared variable in every current template is a platform variable** (`VM_ID`, `HOSTNAME`, `ADMIN_PASSWORD`, the SSH blocks…) — there are zero user-facing variables, so the form would render empty for every template today. User-declared variables are a Marketplace-era concept. Building an empty form now would be UI for data that doesn't exist; deferred to after Marketplace. **Phase-3 Customize is 4 of 5 and as complete as it can be pre-Marketplace** — the remaining gap, plus live-verification of the locked-constraint and OS-pinned tiers, all converge on Marketplace being the honest next move (also §5's own recommendation: it retires the last stale pricing tables and unhardcodes the shell's Deploy button).

**`2026-07-25` · Phase 3 · Owner** — **Deploy Customize scheduling-constraint builder shipped (3 of 5 parity gaps) — the sub-effort of the group, replacing the retired `constraint-builder.js`.** Deploy already accepted `spec.Constraints` and `GET /api/vms/constraint-vocabulary` already served targets/types/operators — but the vocabulary omitted **which operators are valid for which target type**, the one thing a builder needs. **Backend (small, additive):** exposed `OperatorTargetTypes` on the vocabulary DTO, derived from each operator's own `AcceptsTargetType` predicate (the same one `Validate` uses) so the offered set **can't drift from what the server enforces** — the clean fix over hardcoding compatibility client-side. **Frontend:** `ConstraintBuilder` is fully data-driven from the endpoint — target (grouped) → operator (filtered by the target's type) → typed value input (number/boolean/text/comma-list). Two tiers: template-imposed constraints (from `RecommendedSpec.Constraints`) render **read-only** ("set by template"); user rows below. Merged locked+user forward in `customSpec.constraints` (same "customising mustn't drop template defaults" rule as image/bandwidth/GPU). Pure helpers (`operatorsForTarget`, `valueIsList`, `scalarKindForType`, value parse/format) carry the logic and the tests — 114 → 121. **The one client-side bit:** value *arity* (list vs scalar operators) isn't in the vocabulary, so `valueIsList` is a small hardcoded set — it only picks the input widget; the server validates the value, so a wrong guess is a validation error, not corruption. **Verified live:** deployed onto a GPU-present node with a region-in-`eu` constraint — exercising **both** value shapes (Boolean scalar + string list) end to end: vocabulary → filtered operator → typed value → honored placement. **Not yet live: the locked/template tier** — needs a constraint-bearing template, which is Marketplace-era (same shape as OS-pinned images). **Count correction:** this is 3 of 5, not 4 — an earlier note miscounted. Remaining: **replication factor** (still has an unresolved UX-framing decision — ephemeral-vs-durable toggle vs raw replica count) and **template Variables** (Marketplace-entangled: variables are template-declared).

**`2026-07-25` · Phase 3 · Owner** — **Deploy Customize OS-image selector shipped (2 of 5 parity gaps).** `GET /api/system/images` existed but the new form never used it — OS was never a user choice. Added an Operating-system control to Customize whose behavior follows the template, grounded in `PlatformDefaults` + `VmService`: OS-agnostic templates (`RecommendedSpec.ImageId` empty) get a select with "Recommended default" first (sends no `imageId` → server applies the platform default, a **final `VmService` fallback for tenant VMs**, so "" is safe even in customize mode — no default id hardcoded in the client); OS-specific templates (image pinned because their cloud-init assumes it) show it **read-only** so a user can't silently break the setup. One-click is untouched (no `customSpec` → server applies the pin or default). Precedence lives in a pure `resolveImageId(pinned, chosen)` (pinned wins) so the "a pinned template's OS can't be overridden" rule is unit-tested; `os` hint joins the completeness test. **Grounding corrected one of my own assumptions:** I expected customizing an OS-pinned template to drop its image, but `customSpec` already forwarded `rec.imageId` — never a bug, so nothing "fixed" there. 111 → 114 tests. **Verified live:** deployed a VM with **Debian** (not the `ubuntu-22.04` default), and it came up — proving catalogue → select → `customSpec.imageId` → honored server-side. **Not yet live-verified: the OS-pinned read-only branch** — a genuinely pinned template is a Marketplace artifact (Phase 5), so that branch stays unit-tested until Marketplace is integrated. (Flat image list; `optgroup` grouping deferred.) Remaining Customize parity: replication factor, template Variables, scheduling constraints.

**`2026-07-25` · Phase 3 · Owner** — **Deploy Customize description cards shipped (1 of 5 parity gaps).** Legacy Customize showed info cards under each selector (DESIGN §281); the new form had the controls but no help. Added an accurate hint under each of the six controls (vCPU, memory, disk, performance tier, bandwidth, GPU). Copy is **grounded in the real tier semantics and explains the tradeoff, not the label** — the selects already show the ratios (`Standard — 1.6:1 CPU`), so the hint explains what 1.6:1 *means*: `QualityTier` runs inverse (Guaranteed=0 dedicates a core … Burstable=3 packs 4:1), `BandwidthTier` does not, GPU passthrough dedicates a whole card vs proxied sharing one under a VRAM quota. Copy lives in an exported `CUSTOMIZE_HINTS` map next to the tier vocab it's grounded against; a `Hint` component renders it inside the `.field` flex column so it stacks under each control. **Rendered as hint lines, not bordered cards** — stacking a card under every control in the compact grid would fight the form's density; same information, form-appropriate shape. Test is a **completeness invariant** (every customizable field has a non-empty hint), mirroring the "every action has an ordinal" check rather than asserting copy — suite 110 → 111. **Verified live** (screenshot): all six hints render, read cleanly, don't crowd the layout or the price breakdown. Remaining Customize parity: replication factor, OS image, template Variables, scheduling constraints.

**`2026-07-25` · Phase 3 · Owner** — **Direct Access + Custom Domains built in the new app — and building them paid for itself immediately by surfacing a live backend bug.** The stranded modals from the audit below are now real modal-routes off the VM cockpit: `/app/vms/:id/ports` (`DirectAccessModal`, Smart Port Allocation) and `/app/vms/:id/domains` (`DomainsModal`, central-ingress). **Named `/ports`, deliberately not `/access`** — §8 warned `/access` collides with the working `VmAccessInfo` panel and would re-create the ambiguity that hid this regression; `/ports` matches the panel's own "Ports & direct access" label. **Then §6.9 ("the second client is a truth serum") happened for real:** the new UI was the first client to drive `POST /direct-access/quick-add` against a CGNAT VM, and it returned 400. Root-caused across four layers, each real, each fixed at its own boundary — (1) the orchestrator's `WaitForPortAllocationAsync` polled only for success, converting a ~1-second relay *rejection* into a 30-second generic *timeout*; made fail-fast with the real reason surfaced. (2) The actual failure: the relay node's NodeAgent called its **own local** relay VM's `add-port-forward` with no Bearer token, which the relay VM correctly fail-closes (401); fixed by having the node authenticate with the relay-obligation token it already holds locally — no per-command delivery, because the node *hosts* the VM. (3) That token read first missed — a case-sensitive `AuthToken` probe against camelCase `StateJson`, a textbook §6.3 "wire over the doc"; fixed by reading through the shared model. (4) Generalized: every orchestrator `StateJson` (de)serialization unified on `DeCloud.Shared.Json.JsonOptions.Wire`, so casing can't drift across the boundary again. **Verified live:** quick-add SSH allocates `22 → 40000`, and add + remove both work from the UI. Backend fixes carry their own commits; this entry records the frontend closure (§8 Phase-3 gap closed). **The lesson under the bugs:** a feature with no client isn't merely unfinished, it's *unverified at the seam* — the instant Direct Access had a real caller, a 401 that had been acked-and-swallowed silently became visible. Building the UI is what made the backend honest.

**`2026-07-25` · Phase 3 · Owner** — **The "VM-modal cross-module audit" was performed and found a live user-facing regression, not dead code.** §8 had carried this for a day as *"orphaned code, not broken code; low risk; not yet confirmed safe to delete."* Every clause of that was wrong, and the reason is worth more than the finding: **the item was framed as a deletion question, so the audit only ever asked "does anything still call this?"** It does not — and the item's own wording treated that as the reassuring answer. It is the alarming one. **What is actually true:** the 2026-07-24 create-VM-modal and VM-table retirements removed the only entry points to Direct Access and Custom Domains; nothing in the legacy app can open either modal (`openDirectAccessModal` and `openCustomDomainsModal` have zero external callers across all `.js` and `.html`), the modal markup still sits in `index.html` wired to close buttons that nothing can reach, the new app has no replacement whatsoever (`VmDetailPage.tsx` mentions neither; the only match under `wwwroot-next/src/` is the unimported generated `schema.d.ts`), and the backend endpoints are still live and serving nobody. **Users have had no way to configure port mappings or custom domains, in either app, for a day.** **The generalizable error:** an audit inherits the question its tracking item asks. "Is this safe to delete?" and "can a user still reach this?" are answered by the *same grep* and have *opposite* meanings — zero callers means "safe" under the first framing and "broken" under the second. The status-helpers audit two entries above ran the same grep and the deletion conclusion was correct there, because those symbols backed a *retired* page whose replacement had shipped. Here the replacement does not exist. **The distinguishing question is not "who calls this" but "did the thing this served get replaced, or just removed" — check that first, and the caller grep second.** Filed in §8 as a Phase 3 scope gap alongside the Deploy Customize parity work; the fix is a route in the new app, and the legacy modules stay until it ships. **Also found in passing, unrelated:** `window.copyToClipboard` is assigned twice (`app.js:1344` async, `direct-access.js:475` sync) with live consumers in `ssh-wallet.js`'s inline handlers — last script to load wins. Tracked separately in §8.

**Also worth recording: the reason this was hard to see, and hard to explain once seen.** "Access" names two unrelated things — `VmAccessInfo` (SSH/VNC connection details, live on `VmDetailPage`, working) and `VmDirectAccess` (Smart Port Allocation: public subdomain + 40000–65535 port mappings, stranded). The detail page therefore *looks* complete: it has an access panel, it is populated, it updates over SignalR. The first reaction to this finding was reasonably "but we can already see VM details in `/app` — what's the deal?", and that is the whole problem in one sentence. **A shared word across two unrelated features makes a missing one invisible, because the present one answers for it.** The doc's own first draft of this item repeated the error — it claimed port mappings are "how a user reaches SSH", which is false; SSH works through `VmAccessInfo` and the jump host. Corrected. **Also**: whatever route eventually replaces this must not be called `/access`.

**`2026-07-25` · Phase 3 · Owner** — **Tooling repaired, `gen:api` question closed, and a repo-hygiene batch nobody went looking for.** Commits `5781db7`, `8026008`, `24f25d1`, `dd57868`, `791da0e`, `4d6888c`. **Planned work:** `typecheck` fixed from `tsc -b --noEmit` (which failed `TS6310` before reading any source — it had never run) to `tsc -b`, first verdict **0 errors on a forced full check**; `client.ts`'s `ApiResponse<T>` comment rewritten, because it asserted "Enums are string literals (global JsonStringEnumConverter)" as a blanket rule *in the file every feature is told to route through* — invisibly contradicting the three enums that don't, including the write-side trap where `POST /api/vms/{id}/action` needs the ordinal and `{"action":"Stop"}` 400s; `schema.d.ts` gitignored and the adopt-or-drop question settled (see §8); the stale `?page=` sentence removed from `app.js`. **Unplanned, and larger than the planned part — all of it found by reading `git status` output that appeared while checking something else:** `tsconfig.tsbuildinfo` was a **tracked build artifact**, which is not merely untidy — `install.sh` deploys via `git pull` on a persistent checkout, and a modified tracked file aborts that pull mid-deploy. Confirmed live: the very next `git pull` on the server failed with "Your local changes would be overwritten by merge." `appsettings.Production.json` was untracked **and unignored**, one `git add -A` from publication; verified via `git log --all` that it had never been committed, so no credential rotation was needed, and the rule went in the **root** `.gitignore` because a `.gitignore` only governs its own subtree and the file sits a level above `wwwroot-next/`. Two zero-byte files at the repo root (`decloud-app@0.0.0`, `tsc`) created by pasting npm's own output into a shell, where `> decloud-app@0.0.0 typecheck` reads as a redirect. **One self-inflicted bug worth recording:** the first `tsbuildinfo` rule was added with `echo >>` onto a `.gitignore` with no trailing newline, welding it to the previous line and **silently disabling both rules** — `git check-ignore -v` showed one rule where two were expected, which is the only reason it was caught. Use `printf '\n%s\n'` or edit the file. **Everything was verified by the condition that would break it**, not by absence of complaint: after ignoring `tsconfig.tsbuildinfo`, `npx tsc -b --force && git status --short` was run to confirm the tree stays clean *when the file regenerates* — the rule has to survive the file coming back, not just its deletion. **Process failure, now written up as §6.12:** four consecutive commit messages described work their commits did not contain. Nothing was lost — every change exists somewhere in `5781db7`–`4d6888c` — but the history describes a slightly different project than the tree does. The messages were written from the agreed plan rather than from `git show --stat`; the ordering that fixes it is staged-diff first, message second.

**`2026-07-25` · Phase 3 · Owner** — **Build-hygiene batch shipped and verified on srv020184; this entry is the record, because the commits aren't.** Covers commits `c05fd1f`, `f98f59e`, `3c0978c` and `af9be2a`. **What landed:** `RestoreFrontendNext` + legacy twin (lockfile-tracked `npm ci`, see §8), `sourcemap: false`, the `status-helpers.js` retirement, and the duplicated `FrontendSource` line removed from `Orchestrator.csproj`. **Verification, on the production box rather than the WSL checkout** (drvfs timestamp behaviour isn't guaranteed to match ext4, and the whole target turns on mtime comparison): touch the lockfile → build installs (96s) → rebuild skips (6s), install-message count `1` then `0`. **The first test attempted was worthless and worth recording as a trap:** "build twice, expect install-then-skip" cannot fail when `node_modules/.package-lock.json` is *already* newer than the lockfile — it returns `0`/`0` whether the target is wired correctly or missing entirely. A test that can only pass is not a test. The fix was to create the condition being detected (`touch package-lock.json`) so a positive result meant something — the same discipline as §6.1's positive control for negative greps. A cheaper wiring check also exists: `dotnet build -v:n | grep RestoreFrontendNext` prints MSBuild's own "skipping target … up-to-date" line, proving existence, hook-up and Inputs/Outputs evaluation without spending the install. **Process failure worth naming, since the code is fine and the record isn't.** `c05fd1f`'s message described `RestoreFrontendNext` and `sourcemap: false` in detail; **neither was in that commit** — both landed later in `af9be2a`, whose message is `.`. `f98f59e` (the actual `status-helpers.js` deletion) and `3c0978c` (message: a pasted shell transcript) are likewise unusable. Four of six consecutive commits carry no usable description. Nothing to do about pushed history; the durable fix is that this document, not the git log, is where the reasoning lives — and a one-line subject costs nothing next time. **One accidental benefit:** `3c0978c`'s pasted transcript happens to preserve the legacy app's timestamps (`wwwroot/node_modules/.package-lock.json` Jul 24 21:52 vs `wwwroot/package-lock.json` Jun 28 11:28), independently confirming the legacy twin's precondition. **Also decided:** `AGENT_HANDOUT.md` is **no longer maintained** as of this date. Its §8 deploy note still asserts that `BuildFrontendNext` gates `npm ci` on `node_modules` being absent — false since this batch — so treat any conflict between it and this document as this document winning, and don't reintroduce the manual "confirm `npm ci` ran" ritual it describes.

**`2026-07-25` · Phase 3 · Owner** — **Reown dashboard verified; project-ID allowlist populated.** Prompted by noticing that `VITE_WALLETCONNECT_PROJECT_ID` ships in the bundle with — until now — no origin restriction. It is a public client identifier, so this was never a leak; the concern is narrower and worth stating precisely, because it is easy to over- or under-rate. It is **not** phishing protection (a phisher cloning DeCloud would simply register their own project ID in two minutes) and it does **not** hold up any auth invariant — that is held server-side by the SIWE nonce and signature check, which does not involve Reown at all. The actual failure mode is availability: <cite index="4-1">an empty allowlist permits every origin</cite>, so a third party lifting the ID from our bundle can exhaust our relay quota, and the symptom lands on us as *our* users unable to connect wallets, with nothing in our own logs to explain it. **Checked:** project identified and owned; usage/analytics consistent with our own traffic, i.e. no evidence the open ID had actually been used by anyone else — so the exposure was theoretical, which is the useful thing to have established rather than assumed; AppKit feature toggles reviewed (several default to Cloud configuration rather than code, so the dashboard can change app behaviour without a commit); allowlist populated. `WALLETCONNECT_PROJECT_ID` confirmed correct in **both** `wwwroot/.env` and `wwwroot-next/.env.production`. **Watch for on the dev box:** the allowlist must include `http://localhost:3000` and `http://localhost:3001` or the next `npm run dev` fails at the wallet modal, days removed from the change that caused it. **Carry-forward to Phase 6:** deleting the legacy app leaves whichever project ID it used to be retired or re-scoped. Dashboard-side only — no repository artifact exists for any of this, which is the reason for the entry.

**`2026-07-25` · Phase 3 · Owner** — **Correction, in the open: ESLint is installed, passing, and already gating the Release build.** The `2026-07-24` entry below ends "**Not yet installed/run against the codebase or wired into `npm run build`**" — true when written, wrong on all three counts now, and §8 and `AGENT_HANDOUT.md` §3/§7/§8 repeated the stale claim. Grounded state: `eslint@9.39.5` + `eslint-plugin-react-hooks@7.1.1` in `devDependencies` and the lockfile; `"build": "eslint . && tsc -b && vite build"`; a full `npm run build` on the production server passes. **What that buys concretely:** `rules-of-hooks` is `error`, and `BuildFrontendNext` runs `npm run build`, so the next `DeployPage`-shaped hooks violation fails `dotnet build -c Release` — and therefore the deploy — instead of reaching a user. The gate the config was written for is real and armed. **Caveat that survives:** `exhaustive-deps` is `warn` and ESLint exits 0 on warnings, so a green build says *nothing* about what that rule found. Those warnings are the informative half of why the config exists; the config's own comment says several existing effects will trip it and to read them rather than silence them. Run `npm run lint` on its own and record the count. **Process note:** this was found by grounding the docs against the repo rather than by anything failing — the assistant's grounding report called it a live deploy risk ("a lint that has never been run now gates the build"), which was the wrong reading: the gate was already green. The documentation was the only thing broken.

**`2026-07-25` · Phase 3 · Owner** — **`npm audit`: 11 findings, 0 actionable — established by evidence, not by argument.** Prompted by the Phase 1 note that recorded 4 audit findings with a guess ("build-time devDeps likely") that had been carrying weight ever since without being checked. **`npm audit fix` is a no-op here** — it changed nothing and the report was identical afterwards, because both items labelled "fix available via `npm audit fix`" (axios, js-yaml) are transitive and outside their parents' declared ranges. That label means "a fixed version exists," not "this command installs it"; the lockfile was untouched. **Runtime tree (`npm audit --omit=dev`) — three advisories, all closed, each for a different reason:** **axios** (10 advisories, high) reaches the tree via `@reown/appkit-utils` → *optional* `@base-org/account` → `@coinbase/cdp-sdk` → `axios-retry`, and is **absent from `wwwroot/dist-app/assets/*.js`** — nothing the entry point imports reaches it, so Rollup never emits it, including into the lazily-chunked AppKit surfaces (`onramp`, `embedded-wallet`, `pay-with-exchange` were all emitted; none contain it). **`ws`** (2 advisories, high) is the *Node* WebSocket library pulled in by viem under `@reown/appkit-siwe`; browser viem uses the native `WebSocket`, both advisories describe a `ws` **server** receiving hostile frames, and it too is absent from the bundle. **`react-router`** (1 advisory, high) is scoped to **RSC mode**; `grep -rn "rsc\|RSC\|@react-router/server" src/ vite.config.ts` returns nothing, and npm's proposed "fix" is a **downgrade** to 7.11.0 from 7.18.1 — trading a non-applicable advisory for months of real fixes. **Method, and the reason to trust the two absences:** the greps were validated against a positive control — `"Failed to start the connection"` (a SignalR string) matched `index-DDjhnHcL.js`, proving the method could find something before its silence was read as evidence. Folded into §6.1 as a general rule. **Deferred deliberately, dev-only:** esbuild/vite (GHSA-67mh-4wv8-2f99 is the dev-server advisory — exposure is a developer workstation running `vite dev`; srv020184 only ever runs `vite build`; fix is vite@5→8, two majors) and js-yaml via `openapi-typescript` (reached only by `npm run gen:api`, parsing our own swagger output). **Standing rule: `npm audit fix --force` must not be run here** — it would downgrade `@reown/appkit-siwe` to 1.6.5, i.e. roll back the package holding the entire SIWE flow that §6.11's three bugs were spent stabilising. **Expiry condition on the axios finding, corrected from a first wrong statement of it:** the original wording was "recheck if a Coinbase/Base connector or the onramp flow is ever enabled," which confuses a runtime toggle with a build-time fact — the bundle is fixed at build, the onramp and embedded-wallet chunks *were* emitted and still don't contain axios, so flipping a dashboard feature switch cannot pull it in. **The real trigger is the dependency graph moving: if AppKit is upgraded, or `@base-org/account` stops being an optional dependency, rebuild and re-run the grep.** This is a measured property of the emitted output, not a conclusion that carries forward on its own. Also surfaced by the same build, unrelated to the audit: `sourcemap: true` **was** publishing a ~6.3 MB source map of readable source to production (resolved later the same day — see §8), and the main chunk is 1.71 MB (531 kB gzipped). Both tracked in §8.

**`2026-07-25` · Phase 3 · Owner** — **`wwwroot/src/status-helpers.js` retired; an assistant claim corrected in the open.** A grounding pass flagged this file as a *second* live integer-keyed consumer of the `VmStatus` ordinals that §8's blast-radius note had missed, and recommended documenting it. **Wrong — it was already dead**, and the right move was deletion, not documentation. `grep` across `wwwroot/**/*.js` plus the HTML entries found the four symbols (`getStatusClass`, `getStatusText`, `renderServiceReadiness`, `renderServiceBadge`) **only** at their definitions and in `app.js`'s import block; the dashboard and VM-list retirements (2026-07-24) orphaned every call site, no HTML entry references them inline, and no `window.*` re-export exists. Since that import block pulled *only* those four symbols, the entire module was unreachable — so `normalizeServiceStatus`/`getServiceStatusClass`/`getServiceStatusIcon` needed no separate audit. Deleted together: the import block (`app.js` 42–47), the file, and its `Orchestrator.sln` solution-items entry. **Net effect on §8's enum item: the handout's original "the NodeAgent operator dashboard's `vmStateName()`" was right all along, and this deletion makes it right *by construction* rather than by luck.** Second correction from the same pass: `VmNotificationService` hand-builds its SignalR payload with `status.ToString()`, so the push path already sends enum *names* and is untouched by the eventual per-enum-attribute fix — only the REST path moves. **Loose end, deliberately left:** the CSS those functions emitted (`svc-*`, `state-badge`) may now be orphaned in `styles.css` — **not audited, and intentionally so.** `styles.css` belongs to the legacy app and dies wholesale at Phase 6, so the audit buys a few kilobytes for a few weeks; and the class names are built dynamically (`` `svc-${status}` ``, `` `state-badge ${cls}` ``), so a grep-based audit would find nothing for rules that are still in use — a fiddly check with a real chance of deleting something live. The right moment is Phase 6, when the question changes from "is this rule dead?" to the much easier "does the new app need anything from this file before it goes?"

**`2026-07-24` · Phase 3 · Owner** — **Error boundary + ESLint hooks-only config added; this pair of documents + `AGENT_HANDOUT.md` written to bring a fresh agent to full context.** A React Router `errorElement` (`src/app/RouteError.tsx`) now sits on the `/app` root route — catches any child route's thrown error and unmatched-URL 404s with a Meridian-styled page instead of the router's raw developer fallback (the exact page a user saw during the hooks-crash incident, §6.6). Distinguishes "page not found" (stale link) from "something broke" (shows the error message — this is an operator tool, and a message the user can paste into a report is more useful than a generic apology; stack traces stay in the console, no reporting-service hook exists yet). `eslint-plugin-react-hooks` config written (`eslint.config.js`, flat config, ESLint 9) — **scoped deliberately to hooks rules only** (`rules-of-hooks: error`, `exhaustive-deps: warn`), not the full recommended set, because a mature codebase run through a fresh general lint config produces a report nobody reads. **Not yet installed/run against the codebase or wired into `npm run build`** — that's the natural next step, and doing so before the next `DeployPage`-shaped crash is the whole point of having written it.

**`2026-07-24` · Phase 3 · Owner** — **Three separate bugs found and fixed under one user-visible symptom: a spurious wallet-signature modal on every full page load of `/app`.** All three were invisible to `tsc`/`vitest`; all three were found by adding `console.log` instrumentation and reading the actual browser output, after two earlier hypotheses (refresh-token rotation; a call-ordering race) were each disproven by a capture. Full account in §6.11. Summary: (1) `getSession` never actually authenticated — it fetched `/api/auth/session` without an `Authorization` header and always got a 401, for the entire time that code has existed; fixed by having it answer from the app's own restored session state instead of querying the server. (2) The production build's `EXPECTED_CHAIN_ID` silently defaulted to `137` (Polygon **mainnet**) instead of `80002` (Amoy, what the platform runs on) — `VITE_EXPECTED_CHAIN_ID` lived only in a gitignored `.env.local`; fixed with a committed `.env.production`. This almost certainly means `WRONG_NETWORK` derivation has been comparing against the wrong chain in production since Phase 1 — it just never surfaced visibly until AppKit used the same constant to decide session applicability. (3) The session-answering callback read a `useRef` mirror that was still stale at read time because `dispatch()` only schedules a render — fixed by having the restore promise resolve *with* the recovered address rather than depending on a render having landed. **Open follow-up:** the chain ID should be served by the backend, not duplicated into a client build constant with a plausible-wrong fallback.

**`2026-07-24` · Phase 3 · Owner** — **Two legacy retirements executed, ~1,013 lines removed total; nav-of-record handoff (the fiddly moment the plan flagged back in Phase 3's design) has actually happened.** (1) **Legacy dashboard + VM-list pages retired** (~490 lines: `index.html` nav items + page divs; `app.js`'s `loadDashboardStats`/`loadVirtualMachines`/`renderVMsTable`/`attachVmsTableDelegation`/`renderDashboardVMs` and their `window.*` exports — `refreshData` kept but hollowed to `loadUserBalance()` only, since 7+ other call sites and a `window.refreshData` export still need it; `loadNodes()` deliberately preserved, since it sits inside what looked like one contiguous block but is still used by the surviving Nodes page — see §6.7). (2) **Legacy create-VM modal retired** (~523 lines: the modal markup, `openCreateVMModal`/`createVM`/`updateTierInfo`/`updateBandwidthInfo`/`updateReplicationInfo`/`onGpuModeChange`/`updateEstimatedCost`, and — the actual point of doing this now — the **third stale copy** of the tier/bandwidth pricing tables, `QUALITY_TIERS`/`BANDWIDTH_TIERS`/`REPLICATION_TIERS`, which had numbers that didn't even match reality: `pointsPerVCpu: 8/4/2/1` against the backend's actual `16/6.25/2.22/1`. First attempt at this cut deleted `sanitizeVmName`/`validateVmName`/`previewVmName` along with everything else, because they sit physically between the two functions being deleted and are still consumed by other legacy modules — caught and fixed with two separate cuts and an assertion guard, see §6.7. **Hazard flagged for whoever does the next retirement:** five module-scope `document.getElementById('vm-cpu').addEventListener(...)`-style lines (no optional chaining) had to be deleted in the *same* commit as the modal markup, or the whole legacy bundle throws at load and every remaining page — Nodes, Marketplace, Settings, Admin — dies together, not just the retired one.) **Nav-of-record handoff:** `showDashboard()` (the one function both session-restore and fresh sign-in call) now redirects a signed-in `/` visitor to `/app` via `location.replace` (not `assign`, so Back doesn't loop), guarded on the absence of `?page=` so the sidebar's legacy deep-links still work. The new shell's sidebar gained the un-migrated pages (Nodes/Marketplace/My Templates/Settings) as plain `<a href="/?page=x">` — deliberately plain anchors, not `NavLink`, since they leave the SPA — plus an admin section gated on the already-existing, already-tested `canAccessAdmin` guard. Full mechanism and its "two connect flows exist until the `/`-flip" cost documented in `BACKEND_SERVING_SPEC.md` §2.1 (new). `VmsPage` also wired to `useUserRealtime` so the VM list page gets live updates on its own, not only when the dashboard happens to be mounted alongside it — this was the direct fix for a confusing moment where "prove SubscribeToUser" looked like it had failed because the test was run from the wrong page.

**`2026-07-24` · Phase 3 · Owner** — **Marketplace listing payload/reliability fixed — a pre-existing bug unrelated to anything in this migration, found while verifying the retirements above didn't break Marketplace/My Templates/Settings.** `GET /api/marketplace/templates` intermittently returned `200 {"success":true,"data":[]}` or a truncated body. Full root-cause chain and the fix in §6.10 (four independent parts: a new `VmTemplateSummary` DTO — the listing endpoints were shipping the full `CloudInitTemplate` YAML body, ~1 MB total, pushing the driver past a socket timeout; **two separate layers swallowing the resulting exception into an empty success response** rather than letting it become the 500 the controller already knew how to produce; search/limit moved from in-memory to the Mongo query; and a latent seeder bug found as a side effect — it checked for an existing template slug via the *public* catalogue query, which is filtered to `Published`+`Public`, so a `Draft`/`Private` template with the same slug was invisible to it).

**`2026-07-24` · Phase 3 · Owner** — **Tier price multiplier single-sourced; a stuck billing flag self-heals.** Two backend correctness bugs, both with real money consequences, both found by deliberately going looking ("let's handle the bugs first") rather than by a user report. **(1)** `HourlyRateCalculator` (what actually bills) and `SchedulingConfig` (what the node capability model advertises) had disagreed on quality-tier multipliers — `2.5/1.0/0.6/0.4` vs `1.8/1.0/0.7/0.5` — since at least the stored config's creation. An earlier claim in this project that "the legacy UI's tier numbers are stale" was **wrong and corrected in the open**: the legacy UI matches billing exactly; the real disagreement was entirely inside the backend. Fixed by making `HourlyRateCalculator.Calculate` take `SchedulingConfig` as a **required** parameter (not optional-with-a-fallback, which would have silently recreated the same class of bug) and read the multiplier from there; `SchedulingConfigService` gained a version-gated migration (config v1→v2) so the live, already-persisted document converges on load without a manual DB edit. Aligned to the **billed** values deliberately — no bill changes, the advertisement corrects itself; adopting the other set would have repriced the top tier by −28% retroactively. `VmService` and the public `SystemController.CalculatePrice` estimator (what the deploy page's live pricing calls) both now thread the same config through. **(2)** A VM's billing could get stuck `IsPaused=true` (set on insufficient balance) and never resume even after the owner topped up — the resume path only fired on an explicit `BalanceAdded` event that apparently doesn't reliably arrive, and the `VmStop` fall-through bills final usage but leaves the flag set. Fixed with a self-heal: on every billing cycle, if the pause reason is specifically insufficient-balance, probe whether the owner now has funds and clear the flag if so, rather than waiting on an event. **Neither bug had a symptom before the dashboard existed** — `IsPaused` had no reader anywhere in the app until the dashboard's burn-rate calculation excluded paused VMs from the sum, at which point a stale flag started silently understating runway by ~5×.

**`2026-07-24` · Phase 3 · Owner** — **Dashboard shipped (balance/burn-rate/runway, live workload list, promoted Deploy) — and building it found the single biggest latent bug in the project: every VM lifecycle button on the cockpit had been broken since it shipped.** `features/billing/useBalance.ts` (moved out of `features/deploy` once both the fund gate and the dashboard needed it — belongs to neither page specifically), `realtime/useUserRealtime.ts` (first-ever caller of `SubscribeToUser`, which the hub has exposed since it was written with zero publishers until the backend work below), `features/dashboard/DashboardPage.tsx`. Backend: `BalanceResponse.HourlyBurnRate` (sum of `HourlyRateCrypto` across the user's Running, non-paused VMs — computed server-side because `VmSummaryDto` carries no billing info, and a client-side re-derivation would produce a *different* number than what's actually billed); `VmNotificationService.BroadcastStatusAsync` now publishes to **both** `vm:{id}` and `user:{ownerId}` in one call, giving `SubscribeToUser` its first publisher. **While proving the dashboard's live list actually worked, a manual `curl` of the stop-action endpoint (done specifically to trigger a status change without touching the UI) came back `400: "The JSON value could not be converted to VmActionRequest"`.** `VmAction` — the third enum in this API found to lack the per-enum string converter (§6.3) — serializes as a numeric ordinal, and `useVmAction` had been posting `{"action":"Stop"}` as a string since the cockpit was built. **No lifecycle button on the cockpit had ever actually worked**, because every prior "confirmed live" transition in this project had been triggered from the legacy UI, never by clicking the new cockpit's own Stop/Start/Restart/Pause/Resume/ForceStop buttons. Fixed with `vmActionOrdinal` alongside the other two normalizers in `vmStatus.ts`; suite gained 7 tests (110 total) including one asserting every action `allowedActions` can offer has an ordinal mapping. **Separately, the fourth instance of "the mechanism exists, nothing calls it" was found and fixed the same session**, this time for `VmAccessInfoUpdated`: the cockpit's Access card (SSH host, the ready-to-copy `ssh` command) never updated after a VM got its IP, because the only code that ever broadcasts that event is a hub method a *node* would call, and no node ever calls it — the heartbeat path that actually populates `AccessInfo` updated the database and told nobody. Fixed with a new `ApplyReportedAccess` helper (extracted because the same field-setting logic existed verbatim at two heartbeat call sites) returning a change flag, and `BroadcastAccessInfoAsync` (VM-scoped, sends an owner-facing projection — `VmAccessInfo` also carries `VncPassword`, deliberately never sent to the browser). **Testing that fix surfaced one more real gap**: a SignalR reconnection was observed mid-test (a fresh WS connection ID appeared in the Network panel), and because every broadcast in this system is change-gated, a transition that happens entirely inside a disconnect window is lost, not delayed — nothing on the server has anything left to resend. Fixed in `HubProvider`'s `onreconnected` handler, which now invalidates the relevant Query cache keys to force a REST re-sync rather than trusting the socket caught everything. Full account of the four-broadcast pattern in §6.2.

**`2026-07-23` · Phase 3 · Owner** — **Spine largely live: cockpit + real-time + deploy. Four backend gaps found and fixed; lessons folded into §6.** Suite 72→103.

**Shipped.** VM detail cockpit (`VmDetailPage`, owner-facing subset of `VirtualMachine` — status/spec/access/services/timestamps + state-aware lifecycle buttons via `allowedActions`). SignalR real-time (`src/realtime/`: one `HubProvider` connection authed by `accessTokenFactory` → `access_token` query param, since browsers can't set WS `Authorization`; `useVmRealtime` maps `VmStatusChanged`/`VmMetricsUpdated`/`VmAccessInfoUpdated`/`VmServicesUpdated` into the TanStack Query cache — events patch the cache, no parallel state). Metrics panel (REST-seeded from `GET /api/vms/{id}/metrics`, SignalR-updated, **gated on data presence** since the node doesn't push metrics yet). Deploy (`src/features/deploy/`): one-click (no `customSpec` → server applies `RecommendedSpec` *and* its own defaults) + opt-in Customize (cpu/mem/disk, quality tier, bandwidth, GPU+VRAM) + **live price estimate** from `POST /api/system/pricing/calculate` — the same `HourlyRateCalculator` that bills, so the formula is never copied client-side. Runway + fund gate now use the computed cost.

**Backend gaps found (all pre-existing, all latent until a second client walked the paths).** (1) `TransitionAsync` never broadcast status → added `IVmNotificationService` as the one notification seam. (2) The optimistic `Stopping`/`Provisioning` write in `PerformVmActionAsync` bypasses `TransitionAsync` → same seam called from both, gated on real change. (3) Heartbeat updated `vm.Services` without notifying → `BroadcastServicesAsync` (owner-facing projection only; `LastSuccessBody` is guest-controlled text and stays server-side). (4) `platform-general` declared no `ImageId` → empty `BaseImageUrl` → node correctly refused. Fixed at the one creation funnel (`CreateVmAsync`, `PlatformDefaults:DefaultImageId`, `ubuntu-22.04` — the only registry image with a pinned SHA256), **tenant-scoped** so a system VM never silently gets the wrong base OS.

**Also:** `platform-general` given explicit `Min`/`RecommendedSpec` (floors were being read from C# field defaults — see §6.5); bandwidth options decoupled from `defaultBandwidthTier`; `--env-file` documented + guarded (`install.env.example`, and the installer now *rejects* side-effect vars rather than only documenting the rule) after it silently disabled ingress and then produced an unstartable unit.

**Corrections made in the open:** claimed legacy's tier multipliers were stale — they match `HourlyRateCalculator` exactly; the real disagreement is *inside* the backend (`SchedulingConfig` carries a different set). Walked back pinning `ImageId` in the seeder once the OS proved to be a user choice with a default. Walked back a three-file options class when `IConfiguration` was already injected.

**Open follow-ups:** backend tier-multiplier disagreement (`HourlyRateCalculator` 2.5/1.0/0.6/0.4 vs `SchedulingConfig` 0.5/0.7/1.0/1.8 — users can be shown one number and billed by another); Neko template recommends a tier its own `MinimumSpec` forbids; `MinimumSpec` can't express "unconstrained"; node-agent metrics push; `AccessInfo` broadcast unverified (SSH host blank on a Running VM); `eslint-plugin-react-hooks` not installed (would have caught the #310 crash); error boundary + not-found page still open.

**`2026-07-22` · Phase 3 · Owner** — **Step 1a: VM list shipped (paged, 12-state badges), rendering live against real VMs.** First page of the operate spine. `src/features/vms/`: `vmStatus.ts` (pure — `vmStatusBadge` maps all 12 `VmStatus` → 4 tones; `allowedActions(status,powerState,hold)` UX button-gating, server stays the transition authority), `useVms.ts` (TanStack Query — paged `GET /api/vms`, `useVmAction`, `useDeleteVm`), `VmsPage.tsx` (paged table, name→`/vms/:id`, inline-token status badges, loading/error/empty), wired into `routes.tsx` (`/vms`) + `AppShell` nav. 18→ new pure tests; suite 54→72. **Grounding wins:** pulled the ORCHESTRATOR `VmsController` (not NodeAgent's — two exist), `PagedResult<VmSummaryDto>`, the full `VirtualMachine` model, `VmStatus`(12)/`VmPowerState`/`VmAction`, and the controller's `(action,status)` transition rules — did NOT mirror the server's `ValidTransitions` graph client-side (pre-check only). **Live-render finding + fix:** `GET /api/vms` serializes `VmStatus` as a NUMBER (observed `status:3`=Running) — `VmStatus.cs` lacks the `[JsonConverter(JsonStringEnumConverter)]` that `VmRole`/`VmCategory` carry, so it falls through to ordinal. Fixed client-side: `normalizeStatus` tolerates ordinal|numeric-string|name (+ tests). **KNOWN FOLLOW-UP (deferred, has blast radius):** the *correct* fix is adding the attribute to `VmStatus`/`VmPowerState` on the backend to make the enum contract uniform — BUT the legacy NodeAgent `dashboard.js` `vmStateName()` reads integer ordinals, so a backend flip must update that map in the same commit or it breaks. Left as client-tolerance for now. Live: list renders real VM (`llmfit-9530`, Running, 4vCPU/6GB/20GB). Next: 1b VM detail cockpit (reuses `allowedActions`+`useVmAction`/`useDeleteVm`), then retire legacy VM list.

**`2026-07-22` · Phase 2 · Owner** — **Phase 2 COMPLETE — SSH Keys migrated + legacy page RETIRED in production (first strangler retirement).** The shell (`AppShell`/`StatusGate`/gates/`routes.tsx`) is live; SSH Keys built as the first real page (`useSshKeys`, `SshKeysPage`, `AddKeyModal` — Radix, client `validateSshKey` pre-check + surfaces server `INVALID_KEY`), grounded against `UserController` (`/api/user/me/ssh-keys` — NOT the guessed `/api/ssh-keys`, which 404s). Proven live: add/validate/list against the real backend; delete + reload parity. **First retirement executed atomically:** legacy `index.html` nav swung to `/app/settings/ssh-keys`, `#page-ssh-keys` + `#add-ssh-key-modal` deleted; `app.js` (2134→1995 lines) stripped of `loadSSHKeys`/`renderSSHKeysTable`/delegation+cache/`openAddSSHKeyModal`/`addSSHKey`/`deleteSSHKey` + window exports — **carefully scoped**: the VM-SSH-ACCESS feature (ssh-wallet.js, cert flow, jumphost) is a DIFFERENT un-migrated feature, PRESERVED. `node --check` clean. Component CSS (`.dialog-*`/`.btn-*`/`.nav-link`/`.field`) added to `global.css` after a live "invisible modal" bug (Radix opens but no styles) — lesson: ship a component's CSS with it. Deployed to server; renders in dark mode. `EXPECTED_CHAIN_ID` env-wired (`VITE_EXPECTED_CHAIN_ID`). Phase 2 → ☑.

**`2026-07-22` · Phase 2 · —** — **Shell skeleton drafted; combined pure suite green in-repo (48 passed | 5 todo).** Delivered `wwwroot-next/src/app` + `src/features/ssh-keys`. Pure/tested: `resolveShellView` (DerivedStatus → surface; all 6 statuses; only WRONG_NETWORK blocks escrow), `canAccessAdmin` guard, `validateSshKey` — 14 new tests. Skeletons (typed, TODO): `StatusGate`, the four gates (Connect/Reauth/Stale/Network), `AppShell`, React Router v7 `routes.tsx` (admin guard + SSH Keys at `/app/settings/ssh-keys`), `useSshKeys` (TanStack Query), `SshKeysPage`, `AddKeyModal` (Radix). Gate mapping encodes §4: WRONG_NETWORK shows app + escrowBlocked; UNCERTAIN shows app + stale banner; ADDRESS_MISMATCH → reauth as new address (fail closed). Retirement mechanism for SSH Keys spelled out (build → parity → swing legacy link → delete old module, same change). Spec in `src/app/PHASE2_SHELL.md`. **Honest note:** shell stubs import react-router-dom / @tanstack/react-query / @radix-ui/react-dialog → `tsc`/build errors until those are `npm i`-ed; Vitest runs regardless (pure import graph). Phase 2 depends on Phase 1's AuthProvider being wired. Both phases now specified test-first; remaining work is in-repo.

**`2026-07-22` · Phase 1 · Owner** — **walletCrypto cross-compat fixture closed — 54 passed, 0 todo.** Captured a real `{signature, plaintext, ciphertext}` triple from the running legacy app (`wallet-crypto.js`, throwaway wallet) and added it as a fixture test: the noble-v2 port decrypts genuine legacy-produced ciphertext back to plaintext, proven both in a sandbox and in-repo. Cross-compatibility with legacy wallet-encrypted data is now VERIFIED, not assumed — the migration-safety guarantee for VM passwords. Last real Phase 1 item done; `EXPECTED_CHAIN_ID` env-wired earlier and picking the correct chain live; `App.tsx` (Phase 0 proof) deleted; PHASE1_AUTH_CORE.md refreshed to COMPLETE. Remaining are deferrable hardening only (`as never` AppKit-boundary types, `gen:api`→schema.d.ts, clean Release build to exercise BuildFrontendNext, `npm audit` review, favicon). Legacy `app.js` `window.__cap` capture shim to be removed.

**`2026-07-22` · Phase 1 · Owner** — **Phase 1 COMPLETE — full auth flow proven live.** Wallet connect → SIWE nonce → sign → `/api/auth/wallet` verify → `onAuthenticated` → `AUTH_SUCCESS` → `deriveStatus` READY → Meridian shell renders (address chip shows). Reload PERSISTS the session: `dc_rt` httpOnly cookie survives the Vite proxy, AppKit `getSession` restores, and the AuthProvider restore-via-refresh path (mirrored token → callRefresh → AUTH_SUCCESS) re-establishes it; `/api/auth/session` returns `{success:true,data:{address}}`. The AppKit slice — `siwe.ts` (createDecloudSiweConfig), `walletState.ts` (createWalletAdapter), `AuthProvider.tsx` (wiring + restore), `main.tsx` (QueryClient>Auth>Router) — all wired and working against the real orchestrator. Grounding wins this stretch: pulled the real wallet-crypto.js, siwe-config.js, AuthController.cs, app.js, and the actual @noble/@reown installed types before/while writing each port (after early guessing cost a detour). Deps pinned: @noble/ciphers+hashes 2.2.0, @reown/appkit* 1.8.23, ethers 6.17. Deferred/known: `as never` casts at the AppKit boundary (tighten to real types as a hardening pass); `EXPECTED_CHAIN_ID` 137 vs backend Amoy 80002 (set per target chain); walletCrypto cross-compat fixture still todo; App.tsx (Phase 0 proof) now orphaned — delete. Phase 1 → ☑.

**`2026-07-21` · Phase 1 · —** — **Auth-core skeleton drafted and its pure tests pass in-repo (34 passed | 5 todo).** Delivered `wwwroot-next/src/auth` + `src/api`: the pure, fully-tested core is implemented — `deriveStatus` (the §4 truth table, 16 cases), `sessionMachine` reducer + tri-state `performRefresh` (10), `api()` envelope-unwrap + 401 refresh-retry-once + the three failure kinds (8) — plus typed stubs for the pieces needing live AppKit/ethers/backend (`siwe`, `walletCrypto`, `walletState`, `AuthProvider`), each citing the legacy file it ports. `vitest run` green on Windows (Vitest 4.1). One judgment call flagged in code + test: `WRONG_NETWORK` precedes `UNCERTAIN` when both hold. `walletCrypto` + `buildSiweMessage` marked port-verbatim (byte-compat boundaries). `api()` assumes an `ApiResponse<T>` shape — reconcile against generated `schema.d.ts`. Build order + deps in `src/auth/PHASE1_AUTH_CORE.md`. Phase 1 is now scaffolded test-first; remaining Phase 1 work (port the two verbatim modules, wire AppKit + AuthProvider) is in-repo. **Note:** `npm install` reported 4 audit vulns (build-time devDeps likely) — review with `npm audit`, don't reflexively `--force`.

**`2026-07-21` · Pre-req · Owner** — **Serving Change 1 implemented in `Program.cs`** (owner is the `Program.cs` owner). Two surgical, production-only edits, aligned to existing patterns: (1) a `/app` static provider serving `wwwroot/dist-app` at `RequestPath="/app"` with the same cache policy, guarded by its own `Directory.Exists(distAppPath)` (independent of the legacy `dist/`); (2) the existing catch-all `MapFallback` amended with a one-line branch — `/app/*` → `dist-app/index.html`, else `dist/index.html` — keeping the `/api|/hub|/swagger|/health` 404 guard first. **No Development-mode change** (dev serves `/app` from the new app's Vite server). Braces balance; regions verified. **Not yet compiled in-repo** — owner to build. **Still required to finish Phase 0 (not `Program.cs`):** the new Vite project (base `/app/`, output `wwwroot/dist-app`) and the parallel csproj `BuildFrontendNext` target. Change 2 (old-app `?page=`) and Change 3 (`/`-flip) remain for Phase 3 and 4/6.

**`2026-07-21` · Phase 0 · Owner** — **Phase 0 COMPLETE — Release/Production serving verified.** Startup banner in Production confirms all three: `✓ Static files configured: Serving from wwwroot/dist/` (legacy at `/`), `✓ New app configured: Serving /app from wwwroot/dist-app/` (the new `/app` provider activating), `Hosting environment: Production`. Two environment gotchas resolved along the way, both about **build config vs runtime environment being independent knobs**: `-c Release` changes the build, not the environment; and `dotnet run` applies `launchSettings.json` which pinned `ASPNETCORE_ENVIRONMENT=Development` and overrode the shell env var — fixed with `--no-launch-profile` (or running the built DLL). The production-only `/app` static provider + amended `MapFallback` only run when `IsDevelopment()` is false, which is why Production was required to exercise them. Serving Change 1 (Program.cs) proven end to end. Phase 0 → ☑ (dev + production both proven).

**`2026-07-21` · Phase 0 · Owner** — **Scaffold runs — dev pipeline proven.** The Meridian proof page renders at `http://localhost:3001/app/` (Vite): React+TS compiling, `/app` base, tokens + self-hosted fonts loading, light/dark toggle working, `/api`+`/hub` proxying to `:5050`. Setup issues resolved along the way, all pre-existing or environmental, none from the serving/csproj changes: (1) **DI captive dependency** — `IUserService` was needlessly `Scoped` while three singletons consumed it; fixed by registering it `Singleton` (all its deps are singletons, stateless) — a latent bug caught by Dev-mode scope validation. (2) `npm ci` needs a committed `package-lock.json` (`npm install` once → commit). (3) `tsconfig.node.json` can't set `noEmit` under project-references → `emitDeclarationOnly`. (4) Local env: MongoDB not running (started via Docker) + user-secret for the connection string. Non-blocking dev-box noise: `cosign` missing (system-VM seeder) and Caddy `:2019` down (central ingress) — unrelated to frontend. **Still open for Phase 0:** verify the **Release** serving path (`dotnet run -c Release` → `/app` served by ASP.NET from `dist-app`, `/` still legacy) — the production-only `/app` provider + `MapFallback` aren't exercised in dev.

**`2026-07-21` · Phase 0 · —** — **Drafted the Phase 0 scaffold** (`wwwroot-next/`, zipped): Vite + React + TS with `base:'/app/'` → `../wwwroot/dist-app`, dev on `:3001` proxying `/api`+`/hub`(ws)+`/ws` to `:5050`; `index.html` with an inline pre-paint theme script (no flash); `main.tsx` importing fonts (Fontsource) → tokens → global in the right order; `App.tsx` a token-driven Phase 0 proof page with a working light/dark toggle; `design-tokens.css`/`fonts.css`/`global.css` under `src/styles`; `csproj-additions.xml` = a `BuildFrontendNext` target mirroring the existing `BuildFrontend`; README + `.gitignore`; `gen:api` script for OpenAPI types. **Caught + fixed a real bug:** `fonts.css`'s absolute `/fonts/...` urls break under the `/app` base → switched primary fonts to **Fontsource** (base-aware), kept static `fonts.css` as a documented fallback. Not yet `npm install`ed/built in-repo — it's a starter set to drop into `src/Orchestrator/wwwroot-next/`. Phase 0 remains ☐ until it's in the repo, building, and `/app` renders under ASP.NET.

**`2026-07-21` · Phase 0.5 · —** — **Phase 0.5 complete.** Closed the two loose ends. Fonts: `fonts.css` self-hosts the three OFL faces (Space Grotesk / Inter / JetBrains Mono) as woff2 with `font-display:swap` and latin+latin-ext ranges (Cyrillic/CJK deferred to the i18n phase, lazy per-locale); Fontsource npm path documented as the Vite-idiomatic alternative; CDN link dropped for production (kept only in the preview reference). Reconciliation: grounded a read of the existing `wwwroot/design-tokens.css` — it's a *different* system (dark-only, teal, Outfit, glow/gradient, clashing token names/values), so **not replaced in place**. Decision recorded (DESIGN §6.4): Meridian is net-new for the new app; old file stays for legacy during migration; never coexist on a document (serving split); each page adopts Meridian on migration as a restyle (terminal/file-browser → Meridian **dark**); old file retired at cutover. Phase 0.5 → ☑.

**`2026-07-21` · Phase 0.5 · —** — **Built the Meridian token layer** (`design-tokens.css`) — role-based light + derived dark as CSS custom properties. **Contrast computed, not eyeballed:** a WCAG script flagged three real failures the mockup hid (faint text on light 2.7:1, warning text on light 3.6:1, dark tertiary 4.0:1) — all corrected, and it surfaced a genuine tension (three small-text grays can't all clear 4.5:1 on a light canvas), resolved by scoping `--text-tertiary` to large/non-essential text with `--text-secondary` for anything small and informational. Delivered `meridian-reference.html`: components + hero + dashboard rebuilt entirely from tokens (no hardcoded hex) with a live light/dark toggle — the Phase 0.5 exit criteria, visible. Remaining for Phase 0.5: self-host the three OFL fonts as woff2 (drop the CDN link), and reconcile/replace the pre-existing `wwwroot/design-tokens.css` in-repo.

**`2026-07-21` · Phase 0.5 · —** — **Visual direction chosen: "Meridian."** Explored three fresh identities as real standalone HTML mockups (hero + dashboard each), deliberately steered off the three AI-design tells (cream+terracotta, black+acid-green, broadsheet), each re-derived from DeCloud's own world (centerless mesh / beacon / escrow ledger). Picked **A — Meridian**: cool architectural light, iris `#332ED6` accent, Space Grotesk + Inter + JetBrains Mono, centerless-hairline-mesh signature. Added **Phase 0.5 (Visual direction → tokens)** to the plan; token seeds recorded in DESIGN §6.4 (v0.9). Flagged the real consequence: Meridian is light-*first*, not light-*only* — dark mode must be **derived from the same roles** (R7), not skipped. Next: tokenize into `design-tokens.css` (reconciling the one that already exists) + derive dark mode.

**`2026-07-21` · Design · —** — Resolved the surface undercount by grounding `terminal.html`/`file-browser.html` in the code before deciding. Found they're `window.open('_blank')` **pop-out** pages (multi-window is the deliberate affordance), auth token read from `localStorage` (not a URL ticket), plain xterm over WS with **no SharedArrayBuffer/COOP-COEP** (so no app-wide-isolation blocker). Decision: **fold into in-app routes** (`/app/vms/:id/terminal`, `/app/vms/:id/files`) **with pop-out preserved** via a chromeless route variant — elegant *and* nothing deliberate dropped. Also caught a security detail now recorded in DESIGN §10: JWT + VM password ride in the WS query string (browser WS can't set `Authorization`); preserved with a "never log" rule and a backend-dependent short-lived-ticket TODO. Bonus: a `/design-tokens.css` already exists (token layer isn't from scratch). Folded to **DESIGN v0.8**.

**`2026-07-21` · Pre-req · —** — **Grounded the serving spec's §5 checklist against the actual code** (`Program.cs`, `Orchestrator.csproj`, `vite.config.js`, `Orchestrator.sln`); `BACKEND_SERVING_SPEC.md` bumped to **v2**. Two material corrections the grounding caught: (1) **an SPA fallback already exists** — a catch-all `app.MapFallback` delegate (prod-only) that 404s `/api|/hub|/swagger|/health` and serves `dist/index.html` for everything else — so Change 1 must **amend** that fallback (a 3-line `/app` branch), not add a new one; the v1 "no fallback" assumption was wrong. (2) **Surface undercount** — there are six standalone HTML entries, not three: also `terminal.html`, `file-browser.html`, `tos.html` (per-VM operate surfaces + the ToS page). Verdicts: §5.1 grounded w/ corrections; §5.3 grounded (no `/app` backend route collision; amend-fallback fits middleware order); §5.4 grounded and simpler (Debug serves no frontend → **no dev-mode backend change**); only §5.2 folder-names and §5.5 timing need owner sign-off. **Change 1 is now a small, well-scoped edit set; only Change 1 gates Phase 0.** New open item logged (§7): reconcile the surface undercount into DESIGN §4.1/§2/§7.

**`2026-07-21` · Pre-req · —** — Drafted **`BACKEND_SERVING_SPEC.md`** (v1).

**`2026-07-21` · Plan · —** — Implementation plan established at v1.0. **Option A (split by URL) ratified.** Phased approach agreed: scaffold → frozen core → shell+SSH-Keys pipeline proof → spine → landing → supporting → cutover. Retirement mechanism defined (atomic per page, by URL). The nav-of-record handoff (at dashboard migration) identified as the one fiddly execution moment and given a mechanism (old-app deep-link via `?page=`). **Next action: confirm the backend serving split (§3, Pre-req #1) with the `Program.cs` owner before starting Phase 0.** No code written yet.
