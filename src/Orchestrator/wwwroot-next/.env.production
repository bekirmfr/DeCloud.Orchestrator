# Amoy testnet. EXPECTED_CHAIN_ID falls back to 137 (Polygon mainnet) when unset,
# and VITE_EXPECTED_CHAIN_ID lives only in .env.local, which is gitignored — so
# every server build has been shipping mainnet.
VITE_EXPECTED_CHAIN_ID=80002
VITE_WALLETCONNECT_PROJECT_ID=708cede4d366aa77aead71dbc67d8ae5